# CLAUDE.md

MingleAI **v2** — **로테이션 블라인드 소개팅** 앱(정체성 = 한자리에서 여러 사람과 돌아가며·얼굴보다 대화로 먼저·사람을 알아가는 소개팅; 단일 진실 `docs/CONCEPT.md`). 정체성 루프 = **블라인드 스피드 데이트**(곁다리 아님, 앱 그 자체). pnpm monorepo (Node 18+). Native iOS/Android via **React Native + Expo**. AI is limited to preference analysis + matchmaking recommendation — it does **NOT** converse for users (that was v1). **Branch `megahuni` = mainline** (repo 루트에 직접 체크아웃, 2026-07-15~; `main`은 폐기된 v1 아카이브 — v1 README를 믿지 말 것). 상세 스펙: `docs/DEVELOPMENT_PLAN.md`(v2) + `docs/superpowers/plans/*`.

## Maintaining this file

**Whenever a change makes this file stale, update it in the same commit** — commands, ports, env vars, modules, conventions, gotchas, or design tokens. This file is loaded into every session's prompt; treat it as code. A stale CLAUDE.md is worse than none.

Writing tips:
- One line per concept; concrete commands/paths over prose. Brevity = lower token cost every session.
- Document only the non-obvious. Don't restate what the code, README, or `--help` already says.
- Verify against source before writing — never from memory.
- Prefer actionable "do X / don't do Y" gotchas over general description.
- Edit and delete lines that no longer hold; don't just append. 완료 페이즈 상세는 git log/plan 문서로 — 여기엔 살아있는 규칙만.

## Layout

- `apps/mobile` (`@mingle/mobile`) — ⭐ primary client. Expo SDK 56, Expo Router(file-based `app/`), expo-secure-store(JWT), expo-notifications+expo-device(push), expo-image-picker, expo-screen-orientation(매칭~파티 가로 lock), expo-haptics(네이티브 의미 기반 피드백), lucide-react-native + react-native-svg, reanimated 4, 로컬 Pretendard(assets/fonts). (`@expo-google-fonts/gaegu`는 2026-07-23 미사용 — package.json에만 잔존.)
  - **내비**: 비로그인 루트(`app/index.tsx`) → `/login`(**소셜 전용** — 카카오·네이버·구글 + `__DEV__` dev 로그인). 로그인 후 = `(app)/_layout` **게이트 사다리**(`nextGate` 순수함수: 동의`/consent` → 카메라·마이크 권한`/permissions`(하드 차단) → 본인인증`/verify-identity` → 프로필`/onboarding` → 탭). 상세 화면(matching/blocks/chat/party/date-plan/speed-date/report)은 `(app)` Stack이 탭 위로 push. **탭 = 홈·채팅·네이버예약·설정**(2026-07-23, 아이콘 전용; 프로포즈·알림은 `(tabs)/_layout`에서 `href:null`로 바에서 빼고 라우트만 유지 — 홈 허브 '최근' 행에서 push 진입, 각 화면에 back 헤더. 커스텀 `DoodleTabBar`는 `tabBarIcon` 없는 라우트를 스킵). **네이티브 헤더 전면 비표시**(루트 포함) — 뒤로가기는 `src/components/BackButton.tsx`, 브랜딩은 나무늘보 로고 `assets/images/logo.png`.
  - **href 규칙**: 탭 화면은 그룹 탈락형(`/home`, `/chats`, …), 상세 화면은 `(app)` 유지(`/(app)/party/[id]`, …). typed routes는 `expo start`가 재생성 — desync 시 tsc가 잡음.
  - **파티 화면 = 게임 월드**(`party/[id].tsx`): ⚠️ **2026-07-23 비활성** — `src/lib/features.ts` `FEATURES.partyGame=false`(홈 진입점 버튼 disabled '준비 중'·`matching.tsx`가 flag off면 enqueue 스킵 + `<Redirect href="/home"/>`·chats/proposals 빈상태 CTA는 `/speed-date`로). **블라인드 스피드 데이트가 메인**. 픽셀 맵·캐릭터 구현 부담으로 보류 — `party`/`among` 코드·백엔드·테스트 전부 보존(재활성 = 플래그 1줄 `true`). 아래는 재활성 시 스펙: 어몽 세션 활성/직후엔 `AmongGame` 전체화면, 그 외 로비 모드 — 둘 다 공용 `PartyWorld` 렌더러(+반투명 상단 바, 구분선 없음). **넓은 멀티룸 스크롤 카메라 월드**(2026-07-21): 월드가 뷰포트보다 크고 카메라(`worldCamera`)가 내 캐릭터(mine)를 화면 중앙에 두며 스크롤 — 화면 전환·룸 로딩 없음. 7개 룸(라운지·회의실·전기실·무대·정원·댄스홀·기계실)이 벽·문으로 나뉘고 중앙 복도로 전역 연결(풀블리드, 프레임 없음). **게임 월드 전체가 픽셀 아트**(2026-07-21 사용자 지시로 두들 SVG → Kenney 로그라이크 CC0 픽셀로 통일 — 맵·캐릭터 결 일치). 캐릭터 = **Kenney "Roguelike Characters"(CC0) 픽셀 6종**(`DoodleCharacter`가 `<Image>` 렌더 — 단일 포즈, 이동 시 hop, facing scaleX 반전, 유령 반투명, **나=이름표 잉크반전+머리 위 코랄 마커**; profileId 시드로 6종 결정적 `lookFor().seed`). 맵 = **Kenney "Roguelike Indoors"(CC0) 픽셀 타일**: 우드 바닥(`resizeMode="repeat"`)+벽(repeat)+가구 스프라이트(bar/table/sofa/plant/dj피아노/rug)+룸 라벨(`PartyMapArt`, SVG 아님 — View/Image). 에셋 `assets/pixel/`(CC0). ⚠️ RN `<Image>` 픽셀 업스케일 블러 → 소스는 6~10x nearest 업스케일본. 채팅=`PartyChatOverlay`(FAB→중앙 카드), 멤버시트 진입 = 근처 유저 액션패드 "프로필" 또는 상단 멤버 버튼(Users 아이콘, `MemberSheet` — 프로포즈/⋯모더레이션), 밸런스 게임 진입 = DJ 부스 앞 액션패드 "밸런스 게임"→모달. 어몽 미션은 **고대비 공구 마커+최근접 방향/거리+화면 밖 포인터**로 안내하고, 회의는 8인까지 1/2/3열 반응형 선택 카드, 종료는 실제 미션·생존·추방·밸런스 공통답변을 대화 소재로 보여준다.
  - ⚠️ **이동 = 좌측 조이스틱**(PanResponder — 웹 포함 동작; 조이스틱/액션패드는 카메라 스크롤에 고정). **매칭·매칭 성공은 세로, 파티 진입부터 가로 lock**(`useLandscapeLock`는 `party/[id].tsx`만 호출 — 루트는 세로 lock, app.json `orientation: "default"`). 거리·충돌은 world 계량(**x×3.0** = `WORLD_ASPECT`) — 맵/룸/벽/스테이션 단일 진실은 `@mingle/shared` `PARTY_MAP`(백엔드 태스크 배치·AI 이동 공유). **룸 벽 충돌은 클라 전용**(`party-space` `SOLID_WORLD`에 `PARTY_MAP.walls` 편입; 백엔드 AI는 벽 무시·직진, 파인딩 없음 — 백로그).
  - **폰트**: 로컬 Pretendard Regular/SemiBold만 `app/_layout.tsx` `useFonts`로 로드(2026-07-23 Gaegu 폐기; 로드 전 브랜드 로딩 상태). 전 UI Pretendard — `type.*`/`fonts.*` 토큰 사용, 하드코딩 fontFamily 금지.
  - **RN `<Button>` 금지**(플랫폼 기본 파란색이 팔레트 밖) — 항상 `DoodleButton`.
- `apps/backend` (`@mingle/backend`) — NestJS 10 REST + Socket.io gateways(`PartyGateway`/`MessengerGateway`/`SpeedDateGateway`, 기본 네임스페이스), Prisma/PostgreSQL.
- `apps/web` (`@mingle/web`) — Next.js 15.5.21(SWC 동일 버전 고정), ESLint 9 flat config. **v2 역할 = 관리자 대시보드 + 최소 소비자 웹**(대시보드·프로필·예약·데이트플랜·알림·세이프티). v1 소비자 화면(파티 브라우징·3D 뷰어·매칭 리포트)은 2026-07-20 제거 — 파티 경험은 모바일 전용.
- `packages/client-core` (`@mingle/client-core`) — 플랫폼 불문 데이터 계층(API·소켓·zustand auth). **mobile/web보다 먼저 빌드.**
- `packages/shared` (`@mingle/shared`) — 공유 타입. **가장 먼저 빌드**(dual-package — 아래 gotcha).
- `packages/mcp`, `packages/mingleai-mcp` — v1 MCP 서버, v2 제품 경로와 분리(선택적 dev 도구; v1 타입 참조로 빌드 깨져 루트 `pnpm build` 필터에서 제외).

## Commands (repo root)

- `pnpm install` → `pnpm build` (`-r`, v1 MCP 2종 제외 필터; 순서: shared → client-core → apps).
- `pnpm dev:mobile` (`expo start`) / `pnpm dev:backend` / `pnpm dev:web`.
- `pnpm test` (`-r`; client-core=Vitest, backend=jest, mobile=순수 lib 전용 Vitest). `pnpm lint`.
- Prisma (`apps/backend`): `pnpm prisma:migrate`, `pnpm prisma:generate`, `pnpm prisma:studio`.
- **QA 도구**:
  - `node tools/mega-qa.mjs` — 풀퍼널 라이브 전수검사(~20초, 백엔드 :3000 필요). **소셜 전용 전환 후: 백엔드에 `DEV_AUTH_ENABLED=true` + `IDENTITY_DEV_BYPASS=true` 필수**(하네스는 실 OAuth 불가 → dev-login + 본인인증 bypass로 VerifiedGuard 게이트 통과). **재실행 60초 간격**(dev-login 30/min·social 20/min — 어기면 429 연쇄). `/mega-qa` 스킬(`.claude/skills/mega-qa/`)이 절차·오탐 triage 문서.
  - `node tools/seed-demo.mjs <email> <pw>` — 데모 데이터 시더+상주 봇 3명(파티·프로포즈·DM·데이트플랜 시드, 밸런스 자동투표·어몽 슬로우플레이). 매칭 큐는 실행 간 공유 — 잔재 waiting 엔트리가 파티에 섞일 수 있음(설계상 정상).
  - `node tools/among-bots.mjs <count> [--start|--passive] [--hunt]` — 어몽 전용 봇(솔로 테스트). 봇은 항상 crew(임포스터=AI 전용, 2026-07-20~) — `--hunt`는 회의에서 봇/인간이 아닌 프로필(=AI)에 투표.

## Design system — 두 시스템 공존 (line-art + masterpiece) — enforce on ALL new UI

**2026-07-24 재설계 → 전면 다크**: 소프트로즈 폐기, 이후 **전 화면 다크 에디토리얼**(사용자 지시 — 처음 "리스트만 밝게"였다가 탭·리스트·하단네비까지 전부 다크로 확대). = 웜 니어블랙 배경·크림 명조 헤드라인·골드 라벨·크림 pill CTA·르네상스 명화(홈=카페 배경+남녀 캐릭터 레이어 드리프트, 로그인=누끼). 토큰 = `theme.ts` **`dark`**, 화면은 `<AppScreen tone="dark">` + 컴포넌트 `tone="dark"`/`dark` prop(DoodleButton·DoodleCard·DoodleChip·ListRow·LabeledInput·StateView·InlineNotice·ConfirmDialog·PeerModerationMenu·PageHeader). 하단 네비(DoodleTabBar) 다크·활성=골드 아이콘(활성 점 제거). **탭 화면 상단 타이틀 헤더 없음**(홈·chats·settings·naver — header prop 미전달). 라이트 라인아트 `colors`는 이제 안읽음 배지 등 소수 포인트만. spec `docs/superpowers/specs/2026-07-24-lineart-archetype-redesign-design.md`, 정체성 `docs/CONCEPT.md`. **IA = AppScreen 아키타입**(아래). ⚠️ 컴포넌트/토큰 이름은 두들 시대 유지. 라이브 토큰 = `apps/mobile/src/lib/theme.ts`. (명화 크림 `masterpiece`/`MasterpieceHero`는 다크 전환으로 미사용 — 재활용 예약.)

- **골격(라인아트)**: 전 비-Immersive 화면은 **`AppScreen`**(`src/components/AppScreen.tsx`) 단일 래퍼 통과 — SafeArea·`colors.paper`·`PageHeader`·폭캡(`ContentColumn`)·탭 클리어런스·하단고정 `footer`·전폭 `bleed` 슬롯 흡수. 화면은 콘텐츠만. 아키타입 6종 = **Hub / List / Detail / Flow / Sheet / Immersive**(Immersive=스피드데이트 세션·파티 월드만, 골격 밖). 공용 행 = `ListRow`/`RowSeparator`(`src/components/ListRow.tsx`; 카드 내부 `gutter={0}`, 위험행 `tone="danger"`). 빈/로딩/에러 = `StateView` 항상.

> **의도된 예외(되돌리지 말 것)**: 파티 **게임 월드(맵+캐릭터)**는 **Kenney 로그라이크(CC0) 픽셀 아트**(2026-07-21). 단 파티 게임은 현재 `FEATURES.partyGame=false`로 **비활성**(2026-07-23) — 재활성 시에만 관여.

- **Primitives(이름은 두들 시대 유지, 룩은 라인아트)**: `src/components/Doodle.tsx`(`DoodleButton`(primary=`accentStrong` fill+흰글씨/화이트 secondary·`busy` 스피너)/`DoodleCard`/`ShadowBox`) + `DoodleSvg.tsx`(`WobbleBox`=**평범한 라운드 View**(borderRadius+outline 헤어라인; SVG 지터 폐기)/`DoodleFace`(얇은 잉크 라인)/`DoodleChip`(pill, on=`accentStrong`)/`DashedLine`/`MatchGauge` 미사용) + `Foundation.tsx`(`PageHeader`/`StateView`/`ConfirmDialog`/`LabeledInput`/`ContentColumn`/`InlineNotice`) + `AppScreen.tsx`(골격) + `ListRow.tsx` + `MasterpieceHero.tsx`(명화) + `DoodleTabBar.tsx`(`TAB_BAR_ROW_HEIGHT`+`useTabBarClearance()`; 활성 아이콘 코랄·badge=accentStrong) + `DoodleHero.tsx` + `Motion.tsx`(`Enter`/`EnterRow` — reanimated, `useReducedMotion` 존중).
- **파티 게임 월드 컴포넌트**: `src/components/party/`(`PartyWorld` 로비+어몽 공용 렌더러(넓은 멀티룸 스크롤 카메라, 프레임 없음 — `lib/world-view.ts` `worldCamera`)/`PartyMapArt`(**픽셀 타일 맵** — 바닥 repeat·벽 repeat·가구 `<Image>`·룸 라벨, SVG 아님)/`DoodleCharacter`(**Kenney CC0 픽셀 스프라이트 `<Image>`** — SVG 아님)/`Joystick`/`ActionPad`). 캐릭터 배정은 `src/lib/character-look.ts` `lookFor().seed`(profileId 시드 → 6종 결정적; `hair`/`eyes`/`mouth`/`outfit`은 미사용 잔재). 픽셀 에셋 = `assets/pixel/`(Kenney roguelike-indoors·roguelike-characters, CC0 — `KENNEY-LICENSE.txt`). `theme.ts` `shade()`는 현재 미사용(백로그 정리). ~~`furniture/index.tsx`~~ 삭제(두들 SVG 가구 폐기).
- **팔레트(라인아트)**: `ink #181514`(먹선·본문) / `paper #FBFAF8`(웜 오프화이트 배경) / `card #FFFFFF` / `heading #181514`(타이틀도 잉크) / `line #E8E5E0`(=grayLight, divider) / `border`=`outline #E0DBD3`(카드·인풋 외곽) / grays `grayDark #57534E`·`grayMid #78716C`(AA) / fills 중립 `#F4F2EF`·`#ECE9E4`. 포인트 = **코랄 하나**: `accent #FF4D3D`(**선·아이콘·활성탭·틴트 전용 — 흰글씨 AA 실패, 얹지 말 것**), **`accentStrong #D6361F`**(흰글씨 얹는 코랄 fill=버튼·배지, white 4.77:1), `accentDeep/accentPressed #B92E1A`, `accentFill #FFE7E3`, `onAccent #FFF`. 안전=쿨 크림슨 `danger #C4122F`(웜 코랄과 색상 분리)+`dangerFill #FCE8EC`. **elevation=외곽선 우선, 그림자 최소**(`shadow.*`는 모달·시트·hero만; 중립 웜그레이 `#2A2320`). **화면당 primary 1개**. 채팅 버블: 내=`accentStrong`(onAccent), 상대=card+outline.
- **팔레트(명화)**: `masterpiece` 토큰(`theme.ts`) — `cream #F4F1EA`(종이)·`creamDeep #EFE9DC`·`inkDeep #221D18`(명조 헤드라인·블랙 pill)·`inkSoft #6A6151`·`tag #A99A7D`(아이브로우)·`dot #D8CEB9`(하프톤)·`pill/onPill`. 컴포넌트 `MasterpieceHero`+`PillButton`(`src/components/MasterpieceHero.tsx`) — 크림+하프톤·르네상스 누끼 크로스디졸브 모핑·명조 헤드라인·좌→우 크림 스크림·블랙 pill. 누끼 = `assets/images/renaissance-{man,woman}-cutout.png`(그린스크린 소스 `tools/chroma-key.py`로 배경제거·트림). **홈** = 전폭 명화 컴포지션(남자 좌상단·여자 우하단 반전·중앙 MATCH→speed-date).
- **Hierarchy**: 라인아트 primary = `DoodleButton variant="primary"`(accentStrong fill+흰글씨); secondary = 화이트+outline. 위험 `danger`(화이트+크림슨 외곽), 비가역 최종만 `dangerSolid`+`serious`. 명화 primary = `PillButton`(블랙 pill). `busy` = 스피너+차단. 아이콘 = Lucide outline stroke 1.5~1.75. **UI 이모지 금지**(✓·★ 모노만).
- **타이포**: 기능 화면 = **Pretendard**(Regular/SemiBold; display/title/heading 전부 SemiBold, 계층은 크기+코랄). 명화 헤드라인 = **명조 세리프**(`serifFont` in `src/lib/serif.ts` — Platform 격리 모듈이라 theme.ts는 순수 유지; 시스템 폴백 AppleMyungjo/Noto Serif; `assets/fonts/cafe24`에 세리프 `.otf` 드롭 시 그 값 교체). `type.*`/`fonts.*` 토큰만, 하드코딩 fontFamily 금지.
- **RN gotchas**:
  - `WobbleBox`는 이제 순수 라운드 View — SVG 지터/워블 폐기. `doodle-path.ts`(feTurbulence 대체 지터)는 `DoodleFace`·미사용 `MatchGauge`만 사용.
  - 단면 `borderStyle:"dashed"`는 RN 네이티브에서 깨짐(facebook/react-native#24224) — `DashedLine` 사용.
  - `@react-navigation/*` 직접 의존 아님 — 타입은 `expo-router/js-tabs`(BottomTabBarProps)·`expo-router/react-navigation`(useHeaderHeight)에서.
  - RN Web `Switch`는 `trackColor` 객체 무시(기본 그린) — `Platform.OS==="web"`일 때 `activeTrackColor` 스프레드로 교정(notifications.tsx 참조).
  - 내비게이터 기본 배경(#F2F2F2) 노출 방지 — 모든 Stack `screenOptions.contentStyle`에 `colors.paper`.
  - MUI/RN Paper/Material 금지.

## Environment / infra gotchas

- **`.npmrc` `node-linker=hoisted` — 필수**(Metro가 pnpm strict node_modules 해석 불가). 제거 금지. **낡은 `.pnpm` 레이아웃 위에 재설치하면 @nestjs/core 이중 실체로 DI 크래시** — 브랜치 전환 후 이상하면 node_modules 전부 삭제 후 재설치.
- `pnpm install`이 bcrypt 네이티브 바인딩을 지울 수 있음(빌드 스크립트 차단) — `cd node_modules/bcrypt && ../.bin/node-pre-gyp install --fallback-to-build`. better-sqlite3는 빌드 제외됨(v1 mcp 전용; CLT 영수증 없는 맥에서 install 차단 방지).
- Mobile API base: `apps/mobile/.env` `EXPO_PUBLIC_API_URL`. iOS sim `localhost`, Android emu `10.0.2.2`, 실기기 = LAN IP. 릴리즈는 `https://` 필수.
- **jest 30이 pretty-format@30을 루트로 호이스트하면 expo metro-runtime 웹 번들이 크래시** — `apps/mobile` devDep `pretty-format@29.7.0` 고정으로 해결(제거 금지).
- **Postgres :5433**(`docker compose up -d`). DSN `postgresql://mingle:mingle_dev@localhost:5433/mingle`. backend `.env`: `DATABASE_URL`, `JWT_SECRET` 필수; `REDIS_URL` 선택(없으면 인메모리).
- **Docker compose**: 기본 `docker compose up -d` = 인프라만(Postgres·Redis·LiveKit). **`docker compose --profile apps up -d --build`** = backend(:3000, 기동 시 migrate deploy 자동)+web(:3100)까지 컨테이너(dev 핫리로드, `apps/*/src` 마운트; packages 변경 시 재빌드). 모바일 제외. 컨테이너 backend는 `DEV_AUTH_ENABLED`/`IDENTITY_DEV_BYPASS=true`. 이미지 `docker/Dockerfile.dev`(backend·web 공용). 상세 `docker/README.md`. ⚠️ `.dockerignore`에 `**/*.tsbuildinfo` 필수(composite:true tsc가 stale info로 emit 스킵→dist 빈 이미지).
- **`prisma migrate dev/reset/deploy`는 권한 분류기가 차단** — 사용자가 직접 실행(`!` prefix).
- backend TS 에러 grep: ANSI 색코드 때문에 `error TS`가 0으로 나옴 — `"Found N error"`로.
- 포트: backend dev 3000 / PM2 4000(`instances:1` 고정 — redis-adapter 전까지) / web 3100 / Metro 8081.
- **매칭 env**(선택→검증 기본값): `MIN_PARTY_SIZE`(4)/`MAX_PARTY_SIZE`(8)/`MATCH_SWEEP_MS`(2500)/`MATCH_MAX_WAIT_MS`(120000)/`MATCH_BASE_THRESHOLD`(0.5). 스윕은 lifecycle `setInterval` — 단일 인스턴스 전용.
- **어몽 = "AI를 찾아라"(2026-07-20 변형)**: 인간은 **전원 crew** — 임포스터는 **AI 페르소나 전용**(`AMONG_AI_COUNT`명, `ai-` 접두 profileId, 인간 임포스터 선출 로직 제거). AI는 LLM으로 채팅·투표·이동·킬(`AiImpostorBrain`, PartyGateway 1s 스윕에 편입) — `LLM_API_URL` 미설정 시 `AMONG_AI_REQUIRE_LLM=false`여야 시작 가능(그 외엔 템플릿 대사 폴백). `AMONG_AUTO_MEETING_MS` 경과 시 주기 자동 회의 소집(`reason:"auto"`). `isAi`는 ended 스냅샷에만 노출(`isBot`은 어떤 스냅샷에도 미노출 — 서버 내부 전용).
- **어몽 env**(`party/among.config.ts` 검증 기본값): `AMONG_MIN_PLAYERS`(4)/`AMONG_TASKS_PER_CREW`(3)/`AMONG_KILL_RANGE`(0.12)/`AMONG_TASK_RANGE`(0.10)/`AMONG_KILL_COOLDOWN_MS`(20000)/`AMONG_DISCUSSION_MS`(30000)/`AMONG_VOTE_MS`(30000)/`AMONG_EMERGENCY_PER_PLAYER`(1)/`AMONG_SWEEP_MS`(1000)/`AMONG_AI_COUNT`(2)/`AMONG_AUTO_MEETING_MS`(120000)/`AMONG_AI_REQUIRE_LLM`(true)/`AMONG_AI_LLM_MAX_CALLS`(60)/`AMONG_AI_CHAT_MIN_MS`(60000)/`AMONG_AI_CHAT_MAX_MS`(90000). 스윕(PartyGateway lifecycle `setInterval`, 단일 인스턴스 전용)이 회의 타이머·주기 자동 회의·AI 봇 틱(이동/킬/투표/채팅)을 전부 처리.
- **게이트웨이 인메모리 상태**: `PartyGateway`의 `humanPos`(플레이어 좌표 맵)·`chatBuf`(파티별 최근 채팅 링버퍼, AI 발화 컨텍스트용)는 프로세스 메모리 — 단일 인스턴스 전용(redis-adapter 전까지 재시작 시 유실).
- **어몽 자동 시작**: `party:join` 후 프레즌스 로스터==파티 정원 && 이 파티에 among 세션이 **한 번도 없었으면** 자동 시작(`maybeAutoStartAmong`; 밸런스 종료 시에도 구제 재시도). ended 파티는 자동 재시작 안 함(다시하기=수동 `among:start`). 실패는 삼킴 — advisory lock+partial unique가 최종 방어. **도구 영향**: 4소켓 파티 join 즉시 게임 시작 — `game:start`(밸런스)는 among ended 후에만(파티당 ACTIVE `GameSession` 1개, gameType 무관).
- **어몽 승리 규칙(2026-07-15 변경)**: 태스크 승리는 **생존자 소유 태스크만** 요구(사망자 태스크 제외 — doTask/kill/회의해소 3곳 재검사 + 진행률 게이지 동일 기준). 사망 크루 태스크 포함하던 구버전은 교착 버그.
- **CORS env**: `SOCKET_CORS_ORIGINS`(양 게이트웨이+REST 공용 allowlist). 미설정→모든 origin 반사(dev), production이면 부팅 WARN. 모듈 로드 시 읽힘 — 프로세스 시작 전에 설정.
- **Rate limit env**: `RATE_LIMIT_TTL_MS`(60000)/`RATE_LIMIT_MAX`(120). 전역 `HttpThrottlerGuard`(ws 컨텍스트 bypass). 강화: auth social 20/min·dev-login/refresh 30/min, reanalyze 5/min, safety/report 10/min; `/health` 제외. 저장 인메모리 — 단일 인스턴스 전제. **enqueue P2034 소진은 409로 매핑**(재시도 5회+백오프).
- 게이트웨이 앱 에러 이벤트는 네임스페이스드: `party:error` / `messenger:error`(socket.io 예약 `error` 아님) — 개명 시 gateway+client-core+spec 동시 수정.
- Phase-4 env: `PROPOSAL_WINDOW_HOURS`(24)/`PROPOSAL_MAX_PER_PARTY`(3)/`MESSAGE_MAX_LEN`(2000).
- **`@mingle/shared` dual-package**: backend(CJS)가 `preferenceScore` 값을 runtime `require` — shared build가 ESM+CJS 동시 산출. shared 먼저 빌드 안 하면 backend 런타임 실패.
- **push**: `expo-server-sdk`는 ESM-only인데 backend는 CJS — `PushService.ensureExpo()`의 lazy `await import()`(톱레벨 import 금지 — 부팅 크래시). 실패 시 no-op 강등. jest는 `src/__mocks__/`+moduleNameMapper로 목. EAS prod push엔 `app.json` `projectId` 필요(`npx eas-cli init` — 사용자 계정, 런북 §3). `EXPO_ACCESS_TOKEN` 선택.
- **업로드**: `POST /uploads/photo`(JwtAuthGuard, multipart `file`, ≤5MB, magic-byte 검증) → `apps/backend/uploads/`(gitignored), `/uploads/` 정적 서빙. prod는 `PUBLIC_BASE_URL`로 호스트 고정. 모바일 업로드 시 **Content-Type 수동 설정 금지**(fetch가 boundary 유도).
- **블라인드 스피드 데이트**(`apps/backend/src/speed-date/`, `SpeedDateGateway`, 모바일 `app/(app)/speed-date/`, 이벤트 `speeddate:*`): 남3+여3 성별 인지 매칭 → 3단계(DISGUISED 음성변조·아바타 → VOICE 실음성·아바타 → FACE 얼굴)×3라운드 로테이션 → 10초 비공개 상호선택 → Match+DM. **v1 슬라이스 = FACE 1단계만**(`SPEEDDATE_STAGES=1`; `stageOrder=STAGE_ORDER_FULL.slice(-stages)`로 slice=FACE부터 역방향 확장). 기존 party 매칭과 **별도 큐/sweep**(성별 무시하는 party sweep 불침해). **프라이버시 경계 = LiveKit 토큰 grant `canPublishSources`**(FACE 전엔 `["microphone"]`만 — UI 숨김 아님, `@mingle/shared` `stageReveal`이 단일 진실). 세션 상태머신은 `speed-date.state.ts` 순수함수(`nextPhase`/`snapshotFor`/`mutualPairs`) + `SpeedDateSessionService` I/O. 스냅샷 per-viewer 리댁션(별명만; 실명/photo는 매치 후 DM에서만). **미디어는 그린필드** — `LivekitTokenService`(ESM-only, lazy `import(nonLiteral)`; env 미설정 시 빈 토큰=avatar만), 모바일 미디어 seam `src/lib/speed-date-media.ts`(기본 avatar-only; LiveKit 실연동은 EAS dev build 단계 — 런북 `docs/qa/2026-07-22-blind-speed-date-runbook.md`). 아바타는 **비디오 연동 아님**(카메라 미발행 + 정적 이미지). 세션 화면 = **세로**(landscape lock 미호출). 두들 디자인(픽셀 아님).
- **스피드데이트 env**(`speed-date/speed-date.config.ts` 검증 기본값): `SPEEDDATE_STAGES`(1)/`_GROUP_PER_GENDER`(3)/`_PREFLIGHT_MS`(20000)/`_ROUND_MS`(180000)/`_INTERMISSION_MS`(10000)/`_DECISION_MS`(10000)/`_SWEEP_MS`(2500)/`_MAX_WAIT_MS`(120000)/`_BASE_THRESHOLD`(0.4)/`_AI_FILL`(false; dev 전용 슬롯 채움) + `LIVEKIT_URL`/`LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET`(로컬 `docker compose up -d livekit`=devkey/secret). 매칭 sweep(형성)은 `SpeedDateSweepService`, 세션 phase 전이 sweep은 `SpeedDateGateway` lifecycle — 둘 다 단일 인스턴스 전용. enqueue 게이트: 동의·19+·gender∈{male,female}·preferenceSignals.
- **거리 매칭 + 네이버 맛집**(2026-07-23, spec `2026-07-23-location-naver-places`): `SpeedDateQueueEntry`에 `lat/lng/radiusKm`(옵션, ⚠️ **`prisma migrate` 필수** — 미적용 시 큐 쿼리 실패). enqueue DTO `EnqueueSpeedDateDto`가 받고 저장, sweep `pick`이 `@mingle/shared` `withinMutualRadius`(haversine, **더 작은 반경** 기준; 좌표 없으면 무제약)로 그룹 페어 필터. 반경 UI = **2스텝 인트로**(룰 → 풀블리드 지도): 지도 위 플로팅 불투명 pill **일렬**(1/5/10/30km·제한없음, 기본 10; "매칭 거리" 라벨 없음)·좌상단 back·하단 시작하기 오버레이(`speed-date/index.tsx` `RadiusPill`). 위치 = 모바일 시맨 `src/lib/location.ts`(+`.web.ts` = `navigator.geolocation`; native = `expo-location` guarded require; **옵션 권한**, 반경 선택 시에만 요청, 거부 시 무좌표 enqueue). 네이버 맛집 = backend `naver/`(`NaverSearchService` seam — `NAVER_SEARCH_CLIENT_ID/SECRET` 미설정 시 `configured:false`, `GET /places/nearby?query`), 화면 `(tabs)/naver-reserve.tsx`(리스트 + 네이버 링크 `Linking`). 지도 = `src/components/NaverMap.tsx`(native) + `NaverMap.web.tsx`(**웹 프리뷰 = Leaflet+OSM 실지도**, CDN lazy-load, 키 불필요 — 반경 원·핀 렌더). native = `@mj-studio/react-native-naver-map` **guarded require**(설치됨; 네이티브 모듈이라 EAS dev build에서만 렌더, Expo Go/웹 아님), `EXPO_PUBLIC_NAVER_MAP_KEY`(현재 빈값) + **EAS 실기기 전용**. 실제 네이버 '예약' 공개 API 없음 → 링크까지만. env: `NAVER_SEARCH_CLIENT_ID/SECRET`(backend), `EXPO_PUBLIC_NAVER_MAP_KEY`(모바일).

## Data model (v2, `apps/backend/prisma/schema.prisma` — 22 models)

- v2 추가: `MatchmakingQueueEntry`, `PartyMessage`, `GameSession`(밸런스+어몽 겸용, DB-as-state), `Proposal`, `Match`, `DirectMessageRoom`, `DirectMessage`, `Block`, `DeviceToken`, `SpeedDateQueueEntry`(성별 인지 큐), `SpeedDateSession`(DB-as-state, Party 미연결). v1 제거: `Report`, `PartyReservation`, `Profile.agentPersona` 등.
- **partial-unique(손수 SQL 마이그레이션)**: `matchmaking_queue_entries`·`speed_date_queue_entries`는 `(profile_id) WHERE status='waiting'`, `game_sessions`는 `(party_id) WHERE status='active'`.
- 동시성: 용량/카운트 경로는 Serializable tx + `P2034` 재시도 + `P2002` 충돌 처리; 알림은 tx **밖**에서 non-fatal. 게임 상태 전이는 파티별 `pg_advisory_xact_lock` + partial unique(`game_sessions(party_id) WHERE status='active'`, `matchmaking_queue_entries(profile_id) WHERE status='waiting'`).
- 불변식: Match는 `(profileId1,profileId2)` 정렬 정규화 + `upsert` 멱등(try/catch+refetch는 Serializable tx를 abort시킴 — 금지). Block은 양방향(`isBlockedBetween`) — 메신저 send/history/gateway·프로포즈·수락·스윕에서 강제. **피어 프로젝션은 `{profileId,name,age,gender,occupation,photoUrl?,preferenceSummary?}`만**(`toPeer`/`toPublicProfile`) — `userId`/`riskScore`/raw signals 노출 금지.
- DatePlan: 모든 라우트 membership+block 게이트(`memberContext`); `select`=작성자만, `confirm`=상대만(멱등); 응답은 `DatePlanView`(결제 필드 없음).
- 어몽 스냅샷은 per-viewer 리댁션(타인 role은 ended 전까지 null, `isAi`는 ended 전까지 미노출, 내 태스크만, 투표 대상 숨김) — 브로드캐스트는 per-socket personalized.

## Status (요약 — 상세는 git log + docs/superpowers/plans/)

- **현재 상태**: Phases 0–6d + 블라인드 스피드 데이트(메인, v1 슬라이스). 2026-07-23 **비주얼 전면 재설계** — 손그림 두들(순백·블랙 워블·Gaegu)에서 **데이팅 소프트(웜 로즈 종이·화이트 카드·로즈 primary·Pretendard·blur 섀도)**로 전환(P0 토큰→P1 프리미티브→홈/전 화면; spec `2026-07-23-dating-visual-redesign`). + 2026-07-23 **파티 게임(AI 찾기) 비활성 + 블라인드 데이트 메인 전환**(`FEATURES.partyGame=false` — 홈 재구성: 블라인드 데이트 상단 primary 히어로, 게임 파티 '준비 중' disabled; 매칭 라우트 가드; 코드·백엔드·테스트 보존). 2026-07-22 **소셜 전용 인증 전환** — email/password 제거, 카카오·네이버·구글 웹 OAuth + dev-login, **본인인증(실명, 어댑터 seam+dev bypass)**, granular 동의(`ConsentGrant`), 카메라·마이크 하드 게이트, `VerifiedGuard`. 최신 자동화: backend **396/396**, client-core **80/80**, shared **34/34**, mobile **127/127**(총 **637/637**), backend·mobile tsc·루트 lint(경고 0)·루트 production build green. **미적용/미검증**(사용자 실행 필요): DB 마이그레이션(`20260722090000_speed_date`, `20260722120000_social_auth_identity`, **`20260723000000_speed_date_location`** — 이거 미적용 시 speed_date 큐 쿼리가 `column "lat" does not exist`로 크래시) apply, OAuth·본인인증 credential 연결(EXPO_PUBLIC_*_CLIENT_ID + backend keys), 웹/iOS/Android Expo export 재확인, mega-qa 재작성(구 register/login 제거됨), **네이티브 실영상 = EAS dev build + `@livekit/react-native`(+webrtc) 설치**(웹은 `livekit-client`·backend `livekit-server-sdk` 설치완료로 실미디어 동작; 네이티브 seam은 아바타 폴백). **웹 admin 로그인은 prod 백로그**(dev=dev-login). 웹·백엔드·레거시 MCP는 ESLint 9 flat config, Next/SWC 15.5.21.
- **완료 페이즈 한 줄 요약**: P0 스캐폴드+client-core · P1 데이터모델 v2 · P2 온보딩+AI 선호분석(LLM env 미설정 시 stub) · P3 매칭 큐+파티 결성 · P4 프로포즈→매칭→DM(첫 게이트웨이) · P5a push · P5b 데이트플랜 합의 · P5c 모더레이션 UI · P6a 파티 실시간 · P6b 2D 공간 · P6c 밸런스(5라운드, 공개 전 비노출 투표) · P6d 어몽어스 실게임 · 2026-07-14 두들 이식(9task SDD) · 2026-07-15 게임 월드 재구성 + /qa·/design-review 패스(매칭 막다른 화면·어몽 교착 수정, 모션 시스템, 탭 타깃 60px) · 2026-07-20 어몽 → "AI를 찾아라" 변형(인간 전원 crew, 임포스터는 LLM 페르소나 AI 전용, 주기 자동 회의) · 2026-07-21 파티 비주얼 개편 2탄: **넓은 멀티룸 스크롤 카메라 월드**(7룸·벽·문, `WORLD_ASPECT` 1.9→3.0, 벽 충돌 클라 전용, 룸 도달성 flood-fill 검증) + **게임 월드 전체 픽셀 통일**(사용자 지시로 두들 SVG 맵·캐릭터 폐기 → Kenney roguelike-indoors 타일 맵 + roguelike-characters 픽셀 캐릭터, CC0; 중간에 크루메이트+두들얼굴 SVG·Toon 스무스 스프라이트 거쳤으나 맵↔캐릭터 결 통일 위해 픽셀 확정 — `PartyMapArt`/`DoodleCharacter` 모두 View/`<Image>`).
- **남은 출시 항목**: EAS projectId(사용자 `npx eas-cli init`), **실기기 네이티브 E2E**(`docs/qa/2026-07-14-native-e2e-runbook.md` — 두들 체크리스트 §5 포함), refresh token, 관리자 모더레이션 웹.
- **백로그**(비차단): 긴급회의 소진 시 오류문구 미흡·종료 게임 결과 재입장 재노출 정책(제품 판단); push DTO `@ApiProperty` 부재·토글 read-back 없음·cold-start tap 미처리·안읽음 탭 배지; DatePlan `completed` 전이 미구현·결제 잔재 컬럼·레거시 웹 DatePlan 폼(v1 필드, broken); reportUser dedup 잔여 TOCTOU; MatchGauge 미사용(궁합 표시 예약); (KAV 해결 — `AppScreen keyboardAware` prop, 폼 화면 적용); 반응형 저순위 잔여(홈 캐릭터 태블릿 겹침·speed-date 세션 PiP 겹침[LiveKit 실미디어 시]); AI게임 — 게임 중 presence 이벤트 시 AI 캐릭터 ≤1s 깜빡임·meetingSpokeFor 위생 정리·방치 파티 auto 회의가 빈 방에 LLM 발화 소모(세션 만료 정책과 묶어 처리)·Block에 ai- id 시 P2003 500(존재 검사 1개)·AI 이동 가구 충돌 무시.

## Conventions

- Prettier: double quotes, `trailingComma: all`, `printWidth: 100`, semicolons.
- TS: `strict`, `module: Node16`; packages are ESM (`"type": "module"`).
- Korean is used freely in commits, comments, UI copy, and planning docs.
- Auth: **소셜 전용**(email/password 제거). `POST /auth/social`(code→provider 교환, 어댑터 `KakaoProvider`/`NaverProvider`/`GoogleProvider` REST fetch; 미설정=501), `POST /auth/dev-login`(env `DEV_AUTH_ENABLED`, prod 강제 off). 계정 정체성=`(authProvider, providerId)`. `JwtAuthGuard` for user routes, `AdminGuard`+`@Roles` for admin, **`VerifiedGuard`**(본인인증+개인정보동의 필수 — matchmaking·speed-date 진입점에 적용); JWT payload `{ sub, email, role }`. Swagger at `/api`.
- **온보딩 게이트**: 동의(`ConsentGrant` scope=terms|privacy|age19, `POST /auth/consent`) + **본인인증**(실명, `POST /auth/identity/{start,complete}`, 어댑터 seam + `IDENTITY_DEV_BYPASS` 스텁; `identityCi` unique=1인1계정; **verified gender/birth→프로필 age/gender 권위값**, `ProfileService.create`가 반영) + 카메라·마이크 OS 권한. 게이트 판정 = `@mingle/shared` `nextGate(status, perms)`(순수, 클라·서버 공유). `GET /auth/account-status`가 상태 제공. deleteAccount는 비번 없이 confirmation만(소셜=비번 없음).
- 프로세스: 페이즈급 작업은 SDD(plan → per-task review → whole-branch opus review), 회귀는 `/mega-qa`.
