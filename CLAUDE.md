# CLAUDE.md

MingleAI **v2** — **로테이션 블라인드 소개팅** 앱(정체성 = 한자리에서 여러 사람과 돌아가며·얼굴보다 대화로 먼저·사람을 알아가는 소개팅; 단일 진실 `docs/CONCEPT.md`). 정체성 루프 = **블라인드 스피드 데이트**(곁다리 아님, 앱 그 자체). pnpm monorepo (Node 20.18.1+). Native iOS/Android via **React Native + Expo**. AI is limited to preference analysis + matchmaking recommendation — it does **NOT** converse for users (that was v1). **Branch `megahuni` = mainline** (repo 루트에 직접 체크아웃, 2026-07-15~; `main`은 폐기된 v1 아카이브 — v1 README를 믿지 말 것). 상세 스펙: `docs/DEVELOPMENT_PLAN.md`(v2) + `docs/superpowers/plans/*`.

## Maintaining this file

**Whenever a change makes this file stale, update it in the same commit** — commands, ports, env vars, modules, conventions, gotchas, or design tokens. This file is loaded into every session's prompt; treat it as code. A stale CLAUDE.md is worse than none.

Writing tips:
- One line per concept; concrete commands/paths over prose. Brevity = lower token cost every session.
- Document only the non-obvious. Don't restate what the code, README, or `--help` already says.
- Verify against source before writing — never from memory.
- Prefer actionable "do X / don't do Y" gotchas over general description.
- Edit and delete lines that no longer hold; don't just append. 완료 페이즈 상세는 git log/plan 문서로 — 여기엔 살아있는 규칙만.

## Layout

- `apps/mobile` (`@mingle/mobile`) — ⭐ primary client. Expo SDK 56, Expo Router(file-based `app/`), expo-secure-store(JWT), expo-notifications+expo-device(push), expo-image-picker, expo-haptics(네이티브 의미 기반 피드백), lucide-react-native + react-native-svg, reanimated 4, 로컬 Pretendard(assets/fonts). (`@expo-google-fonts/gaegu`는 2026-07-23 미사용 — package.json에만 잔존.)
  - **내비**: 비로그인 루트(`app/index.tsx`) → `/login`(**소셜 전용** — 카카오·네이버·구글 + `__DEV__` dev 로그인). 로그인 후 = `(app)/_layout` **게이트 사다리**(`nextGate(status)` 순수함수 — perms 인자 없음, 2026-07-27: 본인인증`/verify-identity` → 동의`/consent`(terms·privacy 2개 — 만19세 체크 폐기, 나이는 인증이 보장) → 프로필`/onboarding`(닉네임·직업 + **구조화 선호 칩** — 성별·나이 입력 없음) → 탭). **권한은 온보딩이 아니라 필요한 순간에**(2026-08-11): 세션 진입 게이트는 **마이크만**(`speed-date/index` `ensureAvPermissions` → 미허용이면 `/permissions` 프라이밍 push, 허용 후 back), **카메라는 FACE 단계 스냅샷(`room.publishVideo`)에서 세션 화면이 1회 요청**(거부해도 세션 계속 — 발행만 안 됨). 카메라를 진입부터 요구하면 가면·목소리 단계도 못 해보고 이탈한다. 상세 화면(blocks/chat/date-plan/speed-date/report)은 `(app)` Stack이 탭 위로 push. **탭 = 홈·채팅·장소·설정**(아이콘+짧은 라벨; 알림은 홈 우상단 벨 → `NotificationsPopup` 투명 팝업). **네이티브 헤더 전면 비표시**(루트 포함) — 뒤로가기는 `src/components/BackButton.tsx`, 브랜딩은 나무늘보 로고 `assets/images/logo.png`.
  - **href 규칙**: 탭 화면은 그룹 탈락형(`/home`, `/chats`, …), 상세 화면은 `(app)` 유지(`/(app)/speed-date/[id]`, …). typed routes는 `expo start`가 재생성 — desync 시 tsc가 잡음.
  - **폰트**: 로컬 Pretendard Regular/SemiBold만 `app/_layout.tsx` `useFonts`로 로드(2026-07-23 Gaegu 폐기; 로드 전 브랜드 로딩 상태). 전 UI Pretendard — `type.*`/`fonts.*` 토큰 사용, 하드코딩 fontFamily 금지.
  - **RN `<Button>` 금지**(플랫폼 기본 파란색이 팔레트 밖) — 항상 `DoodleButton`.
- `apps/backend` (`@mingle/backend`) — NestJS 10 REST + Socket.io gateways(`MessengerGateway`/`SpeedDateGateway`, 기본 네임스페이스), Prisma/PostgreSQL.
- `apps/web` (`@mingle/web`) — Next.js 15.5.21(SWC 동일 버전 고정), ESLint 9 flat config. **v2 역할 = 공개 랜딩 `/` + 비공개 관리자 콘솔, 그게 전부**(2026-08-12 사용자 결정 — 소비자 웹 화면 `(main)` 프로필·데이트플랜·알림·세이프티 전부 삭제, 소비자 경험은 앱 전용). 라우트는 `/`·법적 3종(`/terms`·`/privacy`·`/account-deletion`)·`/login`(관리자)·`/admin/*`뿐. **admin은 색인 금지**(`app/robots.ts` + `(admin)`/`(auth)` layout `robots:{index:false}`) — 화면 추가 시 같이 챙길 것. 콘솔 테마 = 랜딩과 같은 브랜드 다크(`lib/theme.ts` MUI `mode:"dark"`, 검정 배경·블러시 `#F2BCC8` primary·명조 헤딩 `var(--font-serif)`; v1 핑크 `#E91E63` 폐기). 랜딩 스타일은 `app/page.module.css` 단일 모듈(:root 팔레트 주석 참고). **관리자 화면에 상태·수치를 그릴 땐 실제 응답만** — 예전 대시보드는 "정상" 3줄과 예약/파티 카운트를 하드코딩해 서버가 죽어도 초록이었다.
- `packages/client-core` (`@mingle/client-core`) — 플랫폼 불문 데이터 계층(API·소켓·zustand auth). **mobile/web보다 먼저 빌드.**
- `packages/shared` (`@mingle/shared`) — 공유 타입. **가장 먼저 빌드**(dual-package — 아래 gotcha).
- `packages/mcp`, `packages/mingleai-mcp` — v1 MCP 서버, v2 제품 경로와 분리(선택적 dev 도구; v1 타입 참조로 빌드 깨져 루트 `pnpm build` 필터에서 제외).

## Commands (repo root)

- `pnpm install` → `pnpm build` (`-r`, v1 MCP 2종 제외 필터; 순서: shared → client-core → apps).
- `pnpm dev:mobile` (`expo start`) / `pnpm dev:backend` / `pnpm dev:web`.
- `pnpm test` (`-r`; client-core=Vitest, backend=jest, mobile=순수 lib 전용 Vitest). `pnpm lint`.
- Prisma (`apps/backend`): `pnpm prisma:migrate`, `pnpm prisma:generate`, `pnpm prisma:studio`.
- **QA 도구**:
  - `node tools/mega-qa.mjs` — 풀퍼널 라이브 전수검사(~20초, 백엔드 :3000 필요). **소셜 전용 전환 후: 백엔드에 `DEV_AUTH_ENABLED=true` + `IDENTITY_DEV_BYPASS=true` 필수**(하네스는 실 OAuth 불가 → dev-login + 본인인증 bypass로 VerifiedGuard 게이트 통과). **재실행 60초 간격**(dev-login 30/min·social 20/min — 어기면 429 연쇄). `/mega-qa` 스킬(`.claude/skills/mega-qa/`)이 절차·오탐 triage 문서.

## Design system — 두 시스템 공존 (line-art + masterpiece) — enforce on ALL new UI

**2026-07-24 재설계 → 전면 다크**: 소프트로즈 폐기, 이후 **전 화면 다크 에디토리얼**(사용자 지시 — 처음 "리스트만 밝게"였다가 탭·리스트·하단네비까지 전부 다크로 확대). = 웜 니어블랙 배경·크림 명조 헤드라인·골드 라벨·크림 pill CTA·르네상스 명화(로그인=누끼; **홈은 2026-08-10 재작성 — 명화 컴포지션이 아니라 워드마크+스피드데이트 상태 카드+안전 블록**). 토큰 = `theme.ts` **`dark`**, 화면은 `<AppScreen tone="dark">` + 컴포넌트 `tone="dark"`/`dark` prop(DoodleButton·DoodleCard·DoodleChip·ListRow·LabeledInput·StateView·InlineNotice·ConfirmDialog·PeerModerationMenu·PageHeader). 하단 네비(DoodleTabBar) 다크·활성=골드 아이콘(활성 점 제거). **탭 화면엔 타이틀·설명이 없다**(탭을 누른 순간 어디인지 이미 안다 — 2026-08-11): 홈·settings·naver는 `header` 미전달, chats만 `header={{action}}`로 검색 아이콘 줄만 남긴다. `PageHeader`의 `title`은 optional. 라이트 라인아트 `colors`는 이제 안읽음 배지 등 소수 포인트만. spec `docs/superpowers/specs/2026-07-24-lineart-archetype-redesign-design.md`, 정체성 `docs/CONCEPT.md`. **IA = AppScreen 아키타입**(아래). ⚠️ 컴포넌트/토큰 이름은 두들 시대 유지. 라이브 토큰 = `apps/mobile/src/lib/theme.ts`. (명화 크림 `masterpiece`/`MasterpieceHero`는 다크 전환으로 미사용 — 재활용 예약.)

- **골격(라인아트)**: 전 비-Immersive 화면은 **`AppScreen`**(`src/components/AppScreen.tsx`) 단일 래퍼 통과 — SafeArea·`colors.paper`·`PageHeader`·폭캡(`ContentColumn`)·탭 클리어런스·하단고정 `footer`·전폭 `bleed` 슬롯 흡수. 화면은 콘텐츠만. 아키타입 6종 = **Hub / List / Detail / Flow / Sheet / Immersive**(Immersive=스피드데이트 세션만, 골격 밖). 공용 행 = `ListRow`/`RowSeparator`(`src/components/ListRow.tsx`; 카드 내부 `gutter={0}`, 위험행 `tone="danger"`). 빈/로딩/에러 = `StateView` 항상.


- **Primitives(이름은 두들 시대 유지, 룩은 라인아트)**: `src/components/Doodle.tsx`(`DoodleButton`(primary=`accentStrong` fill+흰글씨/화이트 secondary·`busy` 스피너)/`DoodleCard`/`ShadowBox`) + `DoodleSvg.tsx`(`WobbleBox`=**평범한 라운드 View**(borderRadius+outline 헤어라인; SVG 지터 폐기)/`DoodleFace`(얇은 잉크 라인)/`DoodleChip`(pill, on=`accentStrong`)/`DashedLine`/`MatchGauge` 미사용) + `Foundation.tsx`(`PageHeader`/`StateView`/`ConfirmDialog`/`LabeledInput`/`ContentColumn`/`InlineNotice`) + `AppScreen.tsx`(골격) + `ListRow.tsx` + `MasterpieceHero.tsx`(명화) + `DoodleTabBar.tsx`(`TAB_BAR_ROW_HEIGHT`+`useTabBarClearance()`; 활성 아이콘 코랄·badge=accentStrong) + `DoodleHero.tsx` + `Motion.tsx`(`Enter`/`EnterRow` — reanimated, `useReducedMotion` 존중).
- **다크 구조 대비**(2026-08-06 감사): 글자는 원래 넉넉했지만 면·선이 붙어 카드가 배경에 잠겼다 — `surface #382D23`(카드/배경 1.38:1)·`surfaceHi #473A2E`(선택면/카드 1.22:1)·`border` 0.28·`line` 0.18·**`borderStrong` 0.42**(테두리가 유일 식별 수단인 비활성 칩·고스트 버튼 = 비텍스트 3:1). 블러시(`accent`/`label`)는 아이브로우·활성탭·상태 배지 전용 — 본문성 라벨에 쓰지 말 것. 토큰 변경 시 `src/lib/__tests__/theme.test.ts` 다크 불변식(반투명 합성 포함)이 막는다.
- **다크 팔레트 확장**(2026-08-07 — 배경 하나에 표면 둘뿐이라 화면이 평평했다): 기존 값(bg·surface·surfaceHi·text·pill·accent·danger)은 **앵커로 고정**하고 위에 얹기만 했다. ①**표면 계단** `surfaceTop #544437`(시트·다이얼로그·팝오버) + `surfaceTint`(M3 tonal elevation) — 다크에서 배경보다 어두운 '우물'은 만들지 않는다(bg가 검정에 근접해 대비 1.1을 못 넘음, 들어간 느낌은 테두리로). ②**블러시 램프** `accentBright`/`accent`/`accentDim`/`accentFill`. ③**2차 강조 골드**(`gold` 메타·"예약 가능" 상태)와 **세이지**(`success` 확정·가능) — 블러시(브랜드·활성)와 역할 분리. ④`scrim`/`scrimStrong`/`glow`로 화면마다 흩어진 rgba 통합. 불변식은 `theme.test.ts`(표면 단계 증가·램프 순서·의미색 중복 금지·채움 알파 상한).
- **팔레트(라인아트)**: `ink #181514`(먹선·본문) / `paper #FBFAF8`(웜 오프화이트 배경) / `card #FFFFFF` / `heading #181514`(타이틀도 잉크) / `line #E8E5E0`(=grayLight, divider) / `border`=`outline #E0DBD3`(카드·인풋 외곽) / grays `grayDark #57534E`·`grayMid #78716C`(AA) / fills 중립 `#F4F2EF`·`#ECE9E4`. 포인트 = **코랄 하나**: `accent #FF4D3D`(**선·아이콘·활성탭·틴트 전용 — 흰글씨 AA 실패, 얹지 말 것**), **`accentStrong #D6361F`**(흰글씨 얹는 코랄 fill=버튼·배지, white 4.77:1), `accentDeep/accentPressed #B92E1A`, `accentFill #FFE7E3`, `onAccent #FFF`. 안전=쿨 크림슨 `danger #C4122F`(웜 코랄과 색상 분리)+`dangerFill #FCE8EC`. **elevation=외곽선 우선, 그림자 최소**(`shadow.*`는 모달·시트·hero만; 중립 웜그레이 `#2A2320`). **화면당 primary 1개**. 채팅 버블(2026-08-07): **내=크림 `dark.pill`, 상대=블러시 `dark.accent`**, 둘 다 잉크 글씨(`dark.onPill`) — 좌우 정렬 + 색 두 가지로 화자를 가른다. **시간·읽음은 버블 밖 아래**, **사진은 말풍선 없이 이미지만**.
- **팔레트(명화)**: `masterpiece` 토큰(`theme.ts`) — `cream #F4F1EA`(종이)·`creamDeep #EFE9DC`·`inkDeep #221D18`(명조 헤드라인·블랙 pill)·`inkSoft #6A6151`·`tag #A99A7D`(아이브로우)·`dot #D8CEB9`(하프톤)·`pill/onPill`. 컴포넌트 `MasterpieceHero`+`PillButton`(`src/components/MasterpieceHero.tsx`) — 크림+하프톤·르네상스 누끼 크로스디졸브 모핑·명조 헤드라인·좌→우 크림 스크림·블랙 pill. 누끼 = `assets/images/renaissance-{man,woman}-cutout.png`(그린스크린 소스 `tools/chroma-key.py`로 배경제거·트림). **홈** = 전폭 명화 컴포지션(남자 좌상단·여자 우하단 반전·중앙 MATCH→speed-date).
- **Hierarchy**: 라인아트 primary = `DoodleButton variant="primary"`(accentStrong fill+흰글씨); secondary = 화이트+outline. 위험 `danger`(화이트+크림슨 외곽), 비가역 최종만 `dangerSolid`+`serious`. 명화 primary = `PillButton`(블랙 pill). `busy` = 스피너+차단. 아이콘 = Lucide outline stroke 1.5~1.75. **UI 이모지 금지**(✓·★ 모노만).
- **타이포**: 기능 화면 = **Pretendard**(Regular/SemiBold; display/title/heading 전부 SemiBold, 계층은 크기+코랄). 명화 헤드라인 = **명조 세리프**(`serifFont` in `src/lib/serif.ts` — Platform 격리 모듈이라 theme.ts는 순수 유지; 시스템 폴백 AppleMyungjo/Noto Serif; `assets/fonts/cafe24`에 세리프 `.otf` 드롭 시 그 값 교체). `type.*`/`fonts.*` 토큰만, 하드코딩 fontFamily 금지.
- **RN gotchas**:
  - `WobbleBox`는 이제 순수 라운드 View — SVG 지터/워블 폐기. `doodle-path.ts`(feTurbulence 대체 지터)는 `DoodleFace`·미사용 `MatchGauge`만 사용.
  - 단면 `borderStyle:"dashed"`는 RN 네이티브에서 깨짐(facebook/react-native#24224) — `DashedLine` 사용.
  - `@react-navigation/*` 직접 의존 아님 — 타입은 `expo-router/js-tabs`(BottomTabBarProps)·`expo-router/react-navigation`(useHeaderHeight)에서.
  - RN Web `Switch`는 `trackColor` 객체 무시(기본 그린) — `Platform.OS==="web"`일 때 `activeTrackColor` 스프레드로 교정(notifications.tsx 참조).
  - 내비게이터 기본 배경(#F2F2F2) 노출 방지 — 모든 Stack `screenOptions.contentStyle`에 `colors.paper`.
  - MUI/RN Paper/Material 금지.

## Environment / infra gotchas

- **`.npmrc` `node-linker=hoisted` — 필수**(Metro가 pnpm strict node_modules 해석 불가). 제거 금지. **낡은 `.pnpm` 레이아웃 위에 재설치하면 @nestjs/core 이중 실체로 DI 크래시** — 브랜치 전환 후 이상하면 node_modules 전부 삭제 후 재설치.
- `pnpm install`이 bcrypt 네이티브 바인딩을 지울 수 있음(빌드 스크립트 차단) — `cd node_modules/bcrypt && ../.bin/node-pre-gyp install --fallback-to-build`. better-sqlite3는 빌드 제외됨(v1 mcp 전용; CLT 영수증 없는 맥에서 install 차단 방지).
- Mobile API base: `apps/mobile/.env` `EXPO_PUBLIC_API_URL`. iOS sim `localhost`, Android emu `10.0.2.2`, 실기기 = LAN IP. 릴리즈는 `https://` 필수.
- **jest 30이 pretty-format@30을 루트로 호이스트하면 expo metro-runtime 웹 번들이 크래시** — `apps/mobile` devDep `pretty-format@29.7.0` 고정으로 해결(제거 금지).
- **Postgres :5433**(`docker compose up -d`). DSN `postgresql://mingle:mingle_dev@localhost:5433/mingle`. backend `.env`: `DATABASE_URL`, `JWT_SECRET` 필수; `REDIS_URL` 선택(없으면 인메모리).
- **Docker compose**: 기본 `docker compose up -d` = 인프라만(Postgres·Redis·LiveKit). **`docker compose --profile apps up -d --build`** = backend(:3000, 기동 시 migrate deploy 자동)+web(:3100)까지 컨테이너(dev 핫리로드, `apps/*/src` 마운트; packages 변경 시 재빌드). 모바일 제외. 컨테이너 backend는 `DEV_AUTH_ENABLED`/`IDENTITY_DEV_BYPASS=true`. 이미지 `docker/Dockerfile.dev`(backend·web 공용). 상세 `docker/README.md`. ⚠️ `.dockerignore`에 `**/*.tsbuildinfo` 필수(composite:true tsc가 stale info로 emit 스킵→dist 빈 이미지).
- **`prisma migrate dev/reset/deploy`는 권한 분류기가 차단** — 사용자가 직접 실행(`!` prefix).
- backend TS 에러 grep: ANSI 색코드 때문에 `error TS`가 0으로 나옴 — `"Found N error"`로.
- 포트: backend dev 3000 / PM2 4000(`instances:1` 고정 — redis-adapter 전까지) / web 3100 / Metro 8081.
- **CORS env**: `SOCKET_CORS_ORIGINS`(양 게이트웨이+REST 공용 allowlist). 미설정→모든 origin 반사(dev), production이면 부팅 WARN. 모듈 로드 시 읽힘 — 프로세스 시작 전에 설정.
- **Rate limit env**: `RATE_LIMIT_TTL_MS`(60000)/`RATE_LIMIT_MAX`(120). 전역 `HttpThrottlerGuard`(ws 컨텍스트 bypass). 강화: auth social 20/min·dev-login/refresh 30/min, reanalyze 5/min, safety/report 10/min; `/health` 제외. 저장 인메모리 — 단일 인스턴스 전제. **enqueue P2034 소진은 409로 매핑**(재시도 5회+백오프).
- 게이트웨이 앱 에러 이벤트는 네임스페이스드: `messenger:error`/`speeddate:error`(socket.io 예약 `error` 아님) — 개명 시 gateway+client-core+spec 동시 수정.
- Phase-4 env: `PROPOSAL_WINDOW_HOURS`(24)/`PROPOSAL_MAX_PER_PARTY`(3)/`MESSAGE_MAX_LEN`(2000).
- **`@mingle/shared` dual-package**: backend(CJS)가 `preferenceScore` 값을 runtime `require` — shared build가 ESM+CJS 동시 산출. shared 먼저 빌드 안 하면 backend 런타임 실패.
- **push**: `expo-server-sdk`는 ESM-only인데 backend는 CJS — `PushService.ensureExpo()`의 lazy `await import()`(톱레벨 import 금지 — 부팅 크래시). 실패 시 no-op 강등. jest는 `src/__mocks__/`+moduleNameMapper로 목. EAS prod push엔 `app.json` `projectId` 필요(`npx eas-cli init` — 사용자 계정, 런북 §3). `EXPO_ACCESS_TOKEN` 선택.
- **업로드**: `POST /uploads/photo`(JwtAuthGuard, multipart `file`, ≤5MB, magic-byte 검증) → `apps/backend/uploads/`(gitignored), `/uploads/` 정적 서빙. prod는 `PUBLIC_BASE_URL`로 호스트 고정. 모바일 업로드 시 **Content-Type 수동 설정 금지**(fetch가 boundary 유도).
- **블라인드 스피드 데이트**(`apps/backend/src/speed-date/`, `SpeedDateGateway`, 모바일 `app/(app)/speed-date/`, 이벤트 `speeddate:*`): 남3+여3 성별 인지 매칭 → **각 스테이지 인트로(`stage_intro` phase, "N라운드" 안내+카운트다운)** → 3단계(DISGUISED 음성변조·아바타 → VOICE 실음성·아바타 → FACE 얼굴)×3라운드 로테이션 → 10초 비공개 상호선택 → Match+DM. **기본 풀 3스테이지**(`SPEEDDATE_STAGES` 기본 3; `stageOrder=STAGE_ORDER_FULL.slice(-stages)`로 slice=FACE부터 역방향 축소 — dev/테스트만 1·2로). **phase 머신**(`nextPhase`): `preflight`(조인 윈도우) → `stage_intro` → `round`×3(사이 `intermission`) → 스테이지 경계마다 `stage_intro` → … → `decision` → `ended`. 페어링은 세션 생성 시 성별별 **셔플**(랜덤 매칭; 라운드로빈 불변식 유지 — 각자 상대 전원 1회). **프라이버시 경계 = LiveKit 토큰 grant `canPublishSources`**(FACE 전엔 `["microphone"]`만 — UI 숨김 아님, `@mingle/shared` `stageReveal`이 단일 진실). 세션 상태머신은 `speed-date.state.ts` 순수함수(`nextPhase`/`snapshotFor`/`mutualPairs`) + `SpeedDateSessionService` I/O. 스냅샷에 **`serverNow`**(서버 시각) 포함 — 클라가 기기 시계 오차를 보정해 카운트다운을 센다(에뮬레이터에서 10초 이상 어긋난 사례). **결정/결과 화면**은 전용 전체화면 레이아웃(헤드·중앙 리스트·하단 도크): 결정=전폭 행 탭 선택(카드 그리드 폐기), 결과=상대 아바타 히어로 + 하단 CTA. 큐 대기 시간 초과(`SPEEDDATE_MAX_WAIT_MS`)는 오류가 아니라 `timeout` 상태(다시 찾기/홈으로). 세션 스냅샷이 8초간 안 오면 '홈으로' 탈출구 노출. 스냅샷 per-viewer 리댁션(별명만; 실명/photo는 매치 후 DM에서만). **미디어**: `LivekitTokenService`(ESM-only, lazy `import(nonLiteral)`; env 미설정 시 빈 토큰=avatar만). 모바일 seam = `src/lib/speed-date-media.ts`(**네이티브 = avatar-only**, `@livekit/react-native` EAS dev build 전까지 no-op) + `.web.ts`(**웹 = 실 LiveKit WebRTC**: mic·FACE 카메라 발행, 원격/셀프 트랙 노출). **음성변조**(DISGUISED) = 웹 seam이 mic을 **퍼블리셔측 Web Audio 피치시프트 AudioWorklet**(인라인 blob, 그래뉼러 overlap-add, `pitchRatio` 0.72=변조·1.0=원음)로 통과시켜 발행(원음이 안 나감 = 프라이버시; `voiceMod`은 stage 대칭이라 `partner.voiceMod`가 내 변조 플래그). 실패 시 plain mic 폴백. `modulateVoice` = `useSpeedDateMedia` 3번째 인자. 런북 `docs/qa/2026-07-22-blind-speed-date-runbook.md`. **네이티브 실영상/실음성변조(Phase D/E)는 EAS dev build·실기기 전용**(react-native-webrtc 오디오 처리 = 스파이크). 세션 화면 = **세로**(landscape lock 미호출). **세션 UX(2026-07-27)**: 라운드 화면 하단 1/3 지점에 **추천 질문 30개 crossfade**(`SuggestedQuestion`, 9s 간격·랜덤 순서·reduced-motion 존중); 세션 아바타 = 파스텔 원+**별명 이니셜**(`SpeedDateAvatar` — 두들 얼굴 폐기); 진행 중 이탈은 `beforeRemove` 가드+ConfirmDialog(**BackHandler로는 native-stack 하드웨어 백 못 막음** — beforeRemove 필수); 이탈 후 재진입 = `speed-date/index` `rejoinActiveSession()`(`GET /speed-date/status`의 `{status:"matched",sessionId}`로 복귀; enqueue 409에서도 재시도).
- **스피드데이트 env**(`speed-date/speed-date.config.ts` 검증 기본값): `SPEEDDATE_STAGES`(**3**)/`_GROUP_PER_GENDER`(3)/`_PREFLIGHT_MS`(20000)/`_STAGE_INTRO_MS`(5000)/`_ROUND_MS`(**300000**=5분)/`_INTERMISSION_MS`(5000)/`_DECISION_MS`(10000)/`_SWEEP_MS`(2500)/`_MAX_WAIT_MS`(120000)/`_BASE_THRESHOLD`(0.4)/`_AI_FILL`(false; dev 전용 슬롯 채움) + `LIVEKIT_URL`/`LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET`(로컬 `docker compose up -d livekit`=devkey/secret). 매칭 sweep(형성)은 `SpeedDateSweepService`, 세션 phase 전이 sweep은 `SpeedDateGateway` lifecycle — 둘 다 단일 인스턴스 전용. enqueue 게이트: 동의·19+·gender∈{male,female}·preferenceSignals.
- **데이트 플랜 고도화**(2026-08-06): 만날 지역을 텍스트가 아니라 **지도에서 고른 동네**로 받는다(`(app)/place-area?scope=date` — 맛집 탭 동네와 별도 키에 저장, `src/lib/place-area.ts`). 좌표를 주면 `DatePlanService.attachRealPlaces`가 유형별(`SEARCH_TERMS` cafe/restaurant/walk/museum/movie/concert/activity/bar) 네이버 지역검색으로 **실제 가게**를 코스 칸에 붙인다(`DateStop.place` = 이름·주소·좌표·네이버 지도 URL; 코스 안 중복 가게 금지, 코스 간에는 후보를 돌려 쓴다). 좌표·키 없으면 유형 예시 이름 유지(graceful). 화면은 코스마다 지도(핀) + 칸별 [지도·예약] 링크. **한 매치에 살아 있는 플랜은 하나** — create가 기존 draft를 자동 취소하고, confirmed가 있으면 409.
- **채팅 이미지 첨부**(2026-08-07): `direct_messages.image_url`(마이그레이션 `20260807000000_direct_message_image`, `ADD COLUMN IF NOT EXISTS`). `POST /messenger/rooms/:id/messages`가 `{content, imageUrl?}` — **사진만 보내면 content=""**. ⚠️ `imageUrl`은 `normalizeAttachmentUrl`(=공용 `common/uploads-url.ts` `normalizeUploadUrl`)이 **경로 + 호스트**를 본다 — `PUBLIC_BASE_URL` origin(운영), 미설정 시 루프백·사설 IP만(개발 기기). 2026-08-11 이전엔 경로만 봐서 `https://evil.example.com/uploads/x.png`가 통과했다(추적 픽셀·상대 IP 유출). 프로필 `photoUrl`도 같은 함수(`PROFILE_PHOTO_PATH`). 알림 문구는 "사진을 보냈어요", 채팅 목록 미리보기는 `src/lib/chat-preview.ts`. 업로드는 **XHR**(`client-core` `uploadPhoto`) — Expo SDK 56 fetch가 RN `{uri,name,type}` 파트를 거부한다("Unsupported FormDataPart implementation"). 채팅 첨부는 원본 비율(`pickAndUploadPhoto({square:false})`), 프로필 사진만 1:1 크롭.
- **채팅 답변 추천**(2026-08-06): `POST /messenger/rooms/:id/suggestions`(분당 10회) — 최근 12줄을 **`나/상대` 역할로만 익명화**해 LLM에 보내고 문장 3개를 받는다(`LlmReplySuggester`; 선호분석기와 **LLM 공급자 seam `ai/chat-client.ts`를 공유** — 프롬프트·파싱은 기능이 갖고 HTTP만 클라이언트가 맡는다. **기본 = Anthropic**(`AnthropicChatClient`: `x-api-key`+`anthropic-version`, system은 최상위 필드, `max_tokens` 필수, 응답은 `content[]` text 블록). OpenAI 호환 경로(`OpenAICompatChatClient`, `LLM_*`)는 **제거하지 않고 유지** — `LLM_PROVIDER=openai`로 되돌린다. OpenAI 경로에선 `LLM_REASONING_EFFORT` 설정 시 `temperature` 대신 `reasoning_effort` 전송(gpt-5.x는 temperature 커스텀 값을 거부)). 미설정·실패 시 `@mingle/shared` `suggestReplies` 규칙 폴백(응답 `source`가 `ai`/`fallback`으로 정직하게 구분). 모바일 = 입력창 좌측 ✨ 버튼 → 칩 3개 → 탭하면 **입력창을 채우고 전송은 사용자가** 한다(AI가 대신 대화하지 않는다는 경계 유지).
- **거리 매칭 + 네이버 맛집**(2026-07-23, spec `2026-07-23-location-naver-places`): `SpeedDateQueueEntry`에 `lat/lng/radiusKm`(옵션, ⚠️ **`prisma migrate` 필수** — 미적용 시 큐 쿼리 실패). enqueue DTO `EnqueueSpeedDateDto`가 받고 저장, sweep `pick`이 `@mingle/shared` `withinMutualRadius`(haversine, **더 작은 반경** 기준; 좌표 없으면 무제약)로 그룹 페어 필터. 반경 UI = **2스텝 인트로**(룰 → 풀블리드 지도): 지도 위 플로팅 불투명 pill **일렬**(1/5/10/30km·제한없음, 기본 10; "매칭 거리" 라벨 없음)·좌상단 back·하단 시작하기 오버레이(`speed-date/index.tsx` `RadiusPill`). 위치 = 모바일 시맨 `src/lib/location.ts`(+`.web.ts` = `navigator.geolocation`; native = `expo-location` guarded require; **옵션 권한**, 반경 선택 시에만 요청, 거부 시 무좌표 enqueue). 네이버 맛집 = backend `naver/`(`NaverSearchService` seam — `NAVER_SEARCH_CLIENT_ID/SECRET` 미설정 시 `configured:false`, `GET /places/nearby?query`; ⚠️ **네이버 지역검색은 요청당 최대 5건·페이징 없음**이라 컨트롤러가 질의 변형을 여러 개 던져 합치고 중복 제거한다. **'전체'는 업종 6종(레스토랑·카페·와인바·이자카야·브런치·오마카세)의 합집합**(빈 query/`전체`/`맛집` 모두 전체로 해석) — 예전처럼 `query=맛집` 한 줄이면 그건 또 하나의 좁은 카테고리라 전체(9곳)가 레스토랑(10곳)보다 적었다(2026-08-11). `depth=2`(모바일 '더 보기')는 변형을 늘린다(전체 6→12, 단일 3→7). ⚠️ **변형은 3개씩 끊어 순차 호출** — 한 번에 다 쏘면 네이버가 429를 뱉고 실패한 변형이 조용히 빠져 목록이 반토막 난다. **쿼터 일 25,000회** = 실질 `/places/nearby` 8,300회/일 → `NaverSearchService`가 프로세스 메모리 `TtlCache`로 같은 질의를 재사용(지역검색 6h·지오코딩 24h; 빈 결과도 캐시, 동네 *검색* 빈 결과만 제외). 단일 인스턴스 전제 — 스케일아웃 시 Redis로), 화면 `(tabs)/naver-reserve.tsx`(**캐치테이블 골격 2026-08-11** — 조건 바(동네, 활자만 — 박스로 만들면 입력 필드처럼 보이고 아래 아이콘 버튼과 뎁스 충돌) → 아이콘 버튼(지도·정렬) + 칩(예약 가능만·카테고리) → 결과 수 + 활성 필터 pill(탭해서 해제) → 목록 카드 → '더 보기' → 상세 시트. 지도는 인라인 카드가 아니라 아이콘 버튼 뒤 전체화면. 가게 이름 검색 없음. 사진·평점·가격대는 네이버 지역검색이 주지 않으므로 **만들지 않는다**: 상단 위치 pill → `(app)/place-area` 동네 검색(`GET /places/areas` — **네이버 지역검색 주소를 시/도·시군구로 묶는 게 1순위**, Nominatim은 폴백. OSM은 한국 행정동에 약해 "성수동"이 제주로 잡혔다(2026-08-11); Nominatim 폴백은 부제에 최상위 행정구역을 붙여 오지정을 드러낸다. 선택 위치는 `src/lib/place-area.ts`가 secure-store에 저장 — **현위치가 아니라 지정 동네가 기준**. 기준 좌표가 없으면 검색하지 않고 "동네 고르기" StateView), 지도(`fitKm`으로 결과 핀에 줌 맞춤), 카테고리 칩, 색 타일 카드 + 예약/지도 액션, 탭 시 상세 바텀시트. 예약 링크 판별 = `src/lib/reservation.ts` (`link` 호스트가 캐치테이블·네이버예약·테이블링 등이면 '예약' 배지+직행, 아니면 네이버 장소 페이지 폴백; 호스트 접미사 위장 차단)). 지도 = `src/components/NaverMap.tsx`(native) + `NaverMap.web.tsx`(**웹 프리뷰 = Leaflet+OSM 실지도**, CDN lazy-load, 키 불필요 — 반경 원·핀 렌더). native = `@mj-studio/react-native-naver-map` **guarded require**(설치됨; 네이티브 모듈이라 EAS dev build에서만 렌더, Expo Go/웹 아님), `EXPO_PUBLIC_NAVER_MAP_KEY`(현재 빈값) + **EAS 실기기 전용**. 실제 네이버 '예약' 공개 API 없음 → 링크까지만. env: `NAVER_SEARCH_CLIENT_ID/SECRET`(backend), `EXPO_PUBLIC_NAVER_MAP_KEY`(모바일).

## Data model (v2, `apps/backend/prisma/schema.prisma` — 22 models)

- 핵심 모델: `Match`, `DirectMessageRoom`, `DirectMessage`, `Block`, `DeviceToken`, `SpeedDateQueueEntry`(성별 인지 큐), `SpeedDateSession`(DB-as-state), `DatePlan`, `SafetyReport`, `ConsentGrant`. **2026-08-06 파티 계열 전면 제거**(Party·PartyParticipant·PartyMessage·GameSession·MatchmakingQueueEntry·Proposal + Match.partyId/proposalId + SafetyReport.evidencePartyId — drop 마이그레이션 `20260806000000_drop_party_game`).
- **partial-unique(손수 SQL 마이그레이션)**: `speed_date_queue_entries` `(profile_id) WHERE status='waiting'`.
- 동시성: 용량/카운트 경로는 Serializable tx + `P2034` 재시도 + `P2002` 충돌 처리; 알림은 tx **밖**에서 non-fatal.
- 불변식: Match는 `(profileId1,profileId2)` 정렬 정규화 + `upsert` 멱등(try/catch+refetch는 Serializable tx를 abort시킴 — 금지). Block은 양방향(`isBlockedBetween`) — 메신저 send/history/gateway·스피드데이트 스윕·매치 생성에서 강제. **피어 프로젝션은 `{profileId,name,age,gender,occupation,photoUrl?,preferenceSummary?}`만**(`toPeer`) — `userId`/`riskScore`/raw signals 노출 금지. **프로필 조회 라우트는 없다**(2026-08-11 `GET /profiles`·`GET /profiles/:id` 삭제): 스냅샷의 `partner.profileId`로 조회하면 가면 단계에서 상대 사진이 나왔다. 피어 정보는 매치/DM과 세션 스냅샷만 내려준다. **`PATCH /profiles/:id`는 age/gender를 받지 않는다**(본인인증이 진실 — 예전엔 성별을 뒤집어 반대 성별 큐에 들어갈 수 있었다).
- DatePlan: 모든 라우트 membership+block 게이트(`memberContext`); `select`=작성자만, `confirm`=상대만(멱등); 응답은 `DatePlanView`(결제 필드 없음).

## Status (요약 — 상세는 git log + docs/superpowers/plans/)

- **현재 상태**: 로테이션 블라인드 스피드 데이트 단일 루프. **2026-08-06 파티 게임(AI를 찾아라)·프로포즈 코드 전면 삭제**(사용자 결정 — 모바일 화면·컴포넌트·픽셀 에셋, 백엔드 party/matchmaking/proposal/dashboard 모듈, shared 타입, client-core API/socket, 웹 파티·대시보드·예약 페이지, among-bots·seed-demo 도구, DB 테이블 6종 drop; Match/DM은 스피드데이트 상호선택이 사용). 2026-07-22 소셜 전용 인증(카카오·네이버·구글 + dev-login, 본인인증 seam = `auth/identity/identity-provider.ts`; **공급자 = NICE 하나**(2026-08-12 PortOne 경로 삭제 — 키·계약 없이 실효 0). `nice.provider.ts`는 계약 대기로 **자리만**이라 운영에선 `start()`가 503; 운영 부팅은 `NICE_*` 4개가 있어야 통과) · 2026-07-27 게이트 재정렬(인증→동의→프로필, 권한은 세션 진입 시) · 2026-07-24~ 전면 다크 에디토리얼. 전 스위트 green + tsc 0 + 루트 lint 0. **미적용/미검증**(운영자 단계 — `docs/LAUNCH.md` B): OAuth·본인인증 실키, `ANTHROPIC_API_KEY`(미설정이면 선호분석 stub·답변추천 규칙 폴백), EAS 실기기 E2E, prod 인프라.
- **남은 출시 항목**: **단일 진실 = `docs/LAUNCH.md`**(2026-07-24 작성 — A 완료목록/B 운영자 체크리스트/C 베타 한계). 코드측은 준비 완료(refresh token·모더레이션 웹·eas.json·네이티브 LiveKit 포함); 남은 건 운영자 단계(EAS init·개발자 계정·OAuth/본인인증 실키·인프라·실기기 E2E — 런북 `docs/qa/2026-07-22-blind-speed-date-runbook.md` §7-3, `docs/qa/2026-07-14-native-e2e-runbook.md`).
- **백로그**(비차단): push DTO `@ApiProperty` 부재·토글 read-back 없음·cold-start tap 미처리·안읽음 탭 배지; DatePlan 결제 잔재 컬럼; reportUser dedup 잔여 TOCTOU; 반응형 저순위(홈 캐릭터 태블릿 겹침·speed-date 세션 PiP 겹침[LiveKit 실미디어 시]); 네이티브 음성변조 DSP(Phase E 스파이크 — 현재 DISGUISED에서 마이크 mute 폴백); 위치 스텝 지도 `AppMap`=MapLibre v11(⚠️ `Map`/`GeoJSONSource`/`Layer` 이름 — v10 이름 쓰면 undefined 폴백).

## Conventions

- Prettier: double quotes, `trailingComma: all`, `printWidth: 100`, semicolons.
- TS: `strict`, `module: Node16`; packages are ESM (`"type": "module"`).
- Korean is used freely in commits, comments, UI copy, and planning docs.
- Auth: **소셜 전용**(email/password 제거). `POST /auth/social`(code→provider 교환, 어댑터 `KakaoProvider`/`NaverProvider`/`GoogleProvider` REST fetch; 미설정=501), `POST /auth/dev-login`(env `DEV_AUTH_ENABLED`, prod 강제 off), **`POST /auth/admin-login`**(env `ADMIN_EMAIL`+`ADMIN_PASSWORD_HASH`(bcrypt) — 미설정=501, prod 웹 관리자 콘솔용; `(authProvider="admin", providerId=email)` upsert). 모더레이션 = `/admin/safety-reports`(목록·`:id` 상세·`:id/resolve` 조치=warn/suspend→`resolved`·dismiss)·`/admin/profiles/:id/reinstate`(정지 복구) — suspend는 `profile.status="suspended"`+refresh revoke(=`findActive` 즉시 차단), 웹 `(admin)/admin/reports`. 계정 정체성=`(authProvider, providerId)`. `JwtAuthGuard` for user routes, `AdminGuard`+`@Roles` for admin, **`VerifiedGuard`**(본인인증+개인정보동의 필수 — matchmaking·speed-date 진입점에 적용); JWT payload `{ sub, email, role }`. Swagger at `/api`.
- **온보딩 게이트**(2026-07-27 순서 = 인증→동의→프로필; 권한은 게이트 밖): **본인인증 먼저**(실명, `POST /auth/identity/{start,complete}`, 어댑터 seam + `IDENTITY_DEV_BYPASS` 스텁; dev CI=hash(phone) — 같은 폰번호 재사용 시 409; `identityCi` unique=1인1계정; **만19 미만 403** — 나이 보장은 체크박스가 아니라 인증; **verified gender/birth→프로필 age/gender 권위값**, `ProfileService.create` 반영·dto age/gender는 optional[미인증 레거시용]) + 동의(`REQUIRED_CONSENTS=["terms","privacy"]` — `age19`는 레거시 scope, DTO enum에만 잔존). **카메라·마이크 OS 권한은 스피드데이트 진입 직전 프라이밍으로 요청**(온보딩 드롭오프 방지 — 리서치 근거 `docs/research/2026-07-27-dating-app-ux-analysis.md`). **선호 입력 = 구조화 선택**(2026-08-06 — 자유서술 폐기: 유저가 구체적으로 안 적으면 매칭 신호가 안 됐다). 단일 진실 `@mingle/shared` `preference/catalog.ts`: VIBE/PACE/DRINKING/ACTIVITY 옵션 + `buildPreferenceSignals`(결정적 매핑) + `describePreferences`(한국어 요약, 받침 조사 자동) + `answersFromSignals`(편집 화면 역변환). 클라가 `preferences`를 보내면 서버가 신호·요약문을 확정하고 **AI 분석기를 타지 않는다**(자유서술만 온 레거시 경로에서만 분석기). 공용 UI = `src/components/PreferencePicker.tsx`(온보딩·프로필편집). 게이트 판정 = `@mingle/shared` `nextGate(status)`(순수, 클라·서버 공유). `GET /auth/account-status`가 상태 제공. deleteAccount는 비번 없이 confirmation만(소셜=비번 없음).
- 프로세스: 페이즈급 작업은 SDD(plan → per-task review → whole-branch opus review), 회귀는 `/mega-qa`.

## Deploy Configuration (configured by /setup-deploy)

- Platform: Vercel (`megahuni/web`, root `apps/web`) + Railway API
- Production URL: https://mingles.cloud
- API URL: https://api.mingles.cloud
- Deploy workflow: Vercel CLI for web until the Vercel GitHub integration can access `kwangilkimkenny/MingleAI`; Railway service for API
- Deploy status command: `vercel inspect --scope megahuni https://mingles.cloud`
- Merge method: squash
- Project type: Next.js web + NestJS API + Expo mobile monorepo
- Post-deploy health check: `curl -fsS https://mingles.cloud/ && curl -fsS https://api.mingles.cloud/health && curl -fsS https://api.mingles.cloud/health/ready`

### Custom deploy hooks

- Pre-merge: `pnpm release:check && pnpm --dir apps/mobile dlx expo-doctor@1.20.1`
- Deploy trigger: `vercel deploy --prod --scope megahuni` for web; Railway deployment for API
- Deploy status: `vercel inspect --scope megahuni https://mingles.cloud` and Railway deployment status/logs
- Health check: `https://mingles.cloud`, `https://api.mingles.cloud/health`, `https://api.mingles.cloud/health/ready`
