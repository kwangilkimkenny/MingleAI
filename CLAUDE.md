# CLAUDE.md

MingleAI **v2** — "가벼운 만남" (light/casual meetup) social-matching **mobile app**. pnpm monorepo (Node 18+). Native iOS/Android via **React Native + Expo**. AI is limited to preference analysis + matchmaking recommendation — it does **NOT** converse for users (that was v1). This branch (`worktree-mobile-pivot-plan`) is the v2 go-forward; `main` still holds the abandoned v1 (AI-agent 3D dating sim). Trust `docs/DEVELOPMENT_PLAN.md` (v2) over the v1 README.

## Maintaining this file

**Whenever a change makes this file stale, update it in the same commit** — commands, ports, env vars, modules, conventions, gotchas, or design tokens. This file is loaded into every session's prompt; treat it as code. A stale CLAUDE.md is worse than none.

Writing tips:
- One line per concept; concrete commands/paths over prose. Brevity = lower token cost every session.
- Document only the non-obvious. Don't restate what the code, README, or `--help` already says.
- Verify against source before writing — never from memory.
- Prefer actionable "do X / don't do Y" gotchas over general description.
- Edit and delete lines that no longer hold; don't just append.

## Layout

- `apps/mobile` (`@mingle/mobile`) — ⭐ primary client. Expo SDK 56, Expo Router (file-based `app/`), react-native-reanimated, expo-secure-store (JWT), **expo-notifications + expo-device (Phase 5a push)**. Planned: `@shopify/react-native-skia` for the 2D top-down ("어몽어스") party view.
- `apps/backend` (`@mingle/backend`) — NestJS 10 REST + Socket.io gateway, Prisma/PostgreSQL. Reused from v1, migrated to the v2 domain.
- `apps/web` (`@mingle/web`) — Next.js 15. **v2 role = Admin dashboard + minimal consumer web only.** The v1 3D party viewer is legacy.
- `packages/client-core` (`@mingle/client-core`) — platform-agnostic data layer (API client, socket, auth store via zustand) shared by web + mobile; platform bits (token storage, env) are injected. Vitest tests. **Build before mobile/web.**
- `packages/shared` (`@mingle/shared`) — shared TS types (ESM). **Must build first.**
- `packages/mcp`, `packages/mingleai-mcp` — v1 MCP servers, **decoupled from the v2 product path** (optional dev tools only).

## Commands (repo root)

- `pnpm install` → `pnpm build` (`-r`; build order: shared → client-core → apps).
- `pnpm dev:mobile` (`expo start`) / `pnpm dev:backend` / `pnpm dev:web`.
- `pnpm test` (`-r`; client-core = Vitest, backend = jest `*.spec.ts`). `pnpm lint` (`-r`; backend needs the wired-in `ESLINT_USE_FLAT_CONFIG=false`).
- Prisma (from `apps/backend`): `pnpm prisma:migrate`, `pnpm prisma:generate`, `pnpm prisma:studio`.

## Design system — doodle B&W (mobile-first) — enforce on ALL new UI

Concept: "hand-drawn black & white sketchbook." Cute-yet-simple comes from wonky hand-drawn lines and bold B&W contrast, **not color**. Single source of truth: `apps/web/design/DESIGN.md`; reference impl `apps/web/design/mobile-wireframes.html` (6 mobile screens + bottom tab bar). ⚠️ Those live **only on branch `worktree-doodle-style-tile` (uncommitted, in web/CSS form)** — not yet ported into `apps/mobile`.

- **Pure black & white — zero chroma.** Ink `#17150F` on paper `#FFFFFF`, + 3 grays (`#45413A`, `#8A857C`, `#D9D5CC`) + fills (`#F1EFE9`, `#E7E4DC`). No hue anywhere; even state (예정/진행/완료) differs by fill, not color.
- **Hierarchy without color**: emphasis = **inverted black block + white text** (max 1–2 per screen); density = **black hachure** (`repeating-linear-gradient(45deg)`); highlight = gray underline block, not a color wash.
- **Doodle surface = 4 techniques, zero image assets / zero new deps**: (1) wonky per-corner `border-radius`; (2) `::before` border run through SVG `feTurbulence`→`feDisplacementMap` (border wobbles, text stays crisp); (3) solid offset shadow, **no blur** (`4px 5px 0 #17150F`); (4) slight rotation (−1.5°~2°).
- **RN caveat**: the above are web CSS/SVG techniques. In Expo, re-implement via `react-native-svg` (same feTurbulence/feDisplacementMap) or pre-rendered SVG border assets. **Do NOT use MUI / React Native Paper / Material** — the v2 plan's older "RN Paper" note is superseded by this system.
- **Type**: Gaegu 700 (display/short copy), Pretendard (body), Shantell Sans (en/numeric accent), Gamja Flower (rare scribble). Don't set long Korean body <14px in a handwriting font.
- **Motifs** `DoodleHeart/Sparkle/Face/Connector/Speech/MatchGauge/Spinner`; motion = drawn-in via `stroke-dashoffset`, "boil" sparingly, always honor `prefers-reduced-motion`.

## Environment / infra gotchas

- **`.npmrc` has `node-linker=hoisted` — REQUIRED** for Expo/Metro under pnpm (Metro can't resolve transitive deps in pnpm's strict `node_modules`). Don't remove it.
- Mobile API base: `apps/mobile/.env` → **`EXPO_PUBLIC_API_URL`** (not `NEXT_PUBLIC_*`). Device rules: iOS sim `localhost`, Android emulator `10.0.2.2`, physical device = LAN IP. **Release builds must use `https://`** (cleartext HTTP is blocked on iOS/Android).
- **Postgres on 5433**, not 5432 (`docker compose up -d` → `postgres:15`). DSN `postgresql://mingle:mingle_dev@localhost:5433/mingle`. Backend `.env` needs `DATABASE_URL`, `JWT_SECRET`; optional `REDIS_URL` (→ in-memory fallback).
- **`prisma migrate dev/reset` is blocked by the auto-mode permission classifier** (AI-run DB mutation) — the user must run it (via `!` prefix or a Bash permission rule).
- Checking backend TS errors: `grep "error TS"` falsely returns 0 because ANSI color codes sit between "error" and "TS". Grep `"Found N error"` or strip ANSI first.
- Backend ports: dev `3000` (env `PORT`), PM2/prod `4000` (`ecosystem.config.js`, pinned `instances: 1` until `@socket.io/redis-adapter` is added). Web `3100`.
- Matchmaking env (backend `.env`, all optional → validated defaults): `MIN_PARTY_SIZE`(4, floor 2)/`MAX_PARTY_SIZE`(8)/`MATCH_SWEEP_MS`(2500)/`MATCH_MAX_WAIT_MS`(120000)/`MATCH_BASE_THRESHOLD`(0.5, clamped [0,1]). Sweep is a lifecycle `setInterval` (NOT `@nestjs/schedule`); safe only at `instances:1`.
- Phase-4 env (backend `.env`, optional → validated defaults): `PROPOSAL_WINDOW_HOURS`(24; propose window after a party ends)/`PROPOSAL_MAX_PER_PARTY`(3)/`MESSAGE_MAX_LEN`(2000). Socket.IO gateway (`MessengerGateway`) auto-attaches via `@nestjs/platform-socket.io` (IoAdapter, no `main.ts` change); JWT in the handshake `auth.token`; single-instance only until `@socket.io/redis-adapter`.
- **`@mingle/shared` is dual-package**: the CJS-compiled backend `require()`s the `preferenceScore` *value* at runtime, so shared's `build` emits both ESM (`dist/`) and CJS (`dist/cjs/` via `tsconfig.cjs.json` + a `{"type":"commonjs"}` marker) behind an `exports` `require`/`import` split. Run `pnpm --filter @mingle/shared build` before the backend or its runtime `require` fails.
- Phase-5a push env (backend `.env`, optional): `EXPO_ACCESS_TOKEN` (Expo push security; unset OK in dev). **`expo-server-sdk` is ESM-only (`type:module`) but the backend compiles to CJS** → `PushService` lazy-loads it via `await import()` cached in `ensureExpo()` (NOT a top-level import — that would `ERR_REQUIRE_ESM` and crash boot on Node<20.19); a load failure degrades push to a logged no-op, never fatal. Push is best-effort layered on the in-app `Notification` row (source of truth). Mobile push needs `expo` `projectId` in `app.json` for EAS prod builds (dev/Expo-Go works without); jest mocks expo-server-sdk via `src/__mocks__/` + `moduleNameMapper` (ESM breaks ts-jest). `apps/mobile` has a scoped Vitest for pure libs (`src/lib/__tests__/`).

## Data model (v2, `apps/backend/prisma/schema.prisma` — 17 models)

- Added: `MatchmakingQueueEntry`, `PartyMessage`, `GameSession`, `Proposal`, `Match`, `DirectMessageRoom`, `DirectMessage`, `Block`, **`DeviceToken`** (Phase 5a: `token @unique`, `@@index([userId])`, FK→User Cascade; + `User.pushEnabled Boolean @default(true)`; migration `20260707032116_phase5a_device_tokens`). Removed from v1: `Report`, `PartyReservation`, and `Profile.agentPersona/communicationStyle/values`.
- Concurrency: capacity/count-then-create paths use `prisma.$transaction(..., { isolationLevel: "Serializable" })`; handle `P2034` (retry) + `P2002` (conflict); send notifications **outside** the transaction.
- Established invariants: Match create/lookup **normalizes `(profileId1,profileId2)` ordering** (order-sensitive `@@unique`) and is **idempotent via `upsert`** (Postgres ON CONFLICT — a try/catch+refetch would abort the Serializable tx); accept is Serializable + bounded P2034 retry; **`match_made`/`proposal_received`/`message_received` notifications fire OUTSIDE the tx, non-fatal (try/catch)**. **Blocks are bidirectional (`isBlockedBetween`) and enforced in `MessengerService.memberContext`** (covers send/markRead/history + gateway join/typing via `assertMember`), on proposal send, on `acceptProposal`, and in the sweep. **Peer projections expose only `{profileId,name,age,gender,occupation,photoUrl?,preferenceSummary?}`** — never `userId`/`riskScore`/raw `preferenceSignals` (use `toPeer`/`toPublicProfile`).
- DatePlan agreement (Phase 5b): DatePlan gained `creatorProfileId`/`confirmedAt` (+`cancelled` status). Every `date-plans` route is **membership-bound** (caller's profile ∈ `Match.profileId1/2`, else 403) + **block-gated** (`isBlockedBetween` → 403) via `DatePlanService.memberContext`; `create`/`listForMatch` inline the same checks. **Role separation: `select` = creator only, `confirm` = the peer only** (status-guarded `updateMany(where status="draft")` → idempotent double-confirm; null `creatorProfileId` non-actionable). `GET /date-plans/:id` is now authz'd (was public IDOR). All endpoints return `DatePlanView` (no payment fields); `reservation`-type notifications (best-effort) carry `data:{datePlanId,matchId}` and deep-link mobile via `routeForNotification`. The hardcoded course generator + unused payment columns are untouched.
- Carry-forward (later phases): ~~dashboard `getSummary/getMyParties` IDOR~~ **FIXED (QA 2026-07-10, 892591f)** — profileId now JWT-derived, mismatched query param → 403; backlog (surfaced by Phase-4 QA) — double `match_made` on mutual cross-accept (dedupe by matchId), reportUser dedup residual TOCTOU + gateway CORS `origin:true` allowlist before prod. Phase-5a backlog: push DTOs lack `@ApiProperty`; the mobile push toggle only *sets* `pushEnabled` (never reads it back — no getMe wiring); no cold-start tap handling (`getLastNotificationResponseAsync`); `app.json` `projectId` for EAS prod push; unread tab-bar badge + foreground-suppression deferred. Phase-5b backlog: DatePlan `completed` transition unimplemented; vestigial payment columns not removed; legacy web DatePlan form still sends v1 `profileId1/2` (broken, out of scope); typed-route `as any` casts self-heal on `expo start` typegen.

## Status (branch `megahuni`, not merged to main; Phases 0–6c pushed to origin)

- **전수 QA (2026-07-10, whole app, 892591f pushed)**: 4-way adversarial hunt (authz/privacy · concurrency/state-machine · mobile all-screens · cross-layer contract) + a **full-funnel live E2E 57/57** (signup→queue→party→chat/move→balance-game incl. simultaneous vote+start races→propose→accept→match→DM→date-plan→report/block+enforcement→dashboard IDOR) vs a booted backend + real Postgres. Found & fixed 3 code-only defects: dashboard IDOR (QA-1), GameService RMW race (QA-2), matching retry dead-button (QA-3) — see the crossed-out backlog items below. Green after fixes: backend jest **185/185 (21 suites)**, backend build clean, mobile tsc clean + vitest **22/22**. Contract audit: all 26 WS events + 12 client-core REST wrappers agree across 4 layers. Remaining backlog (migration-gated / deferred): enqueue can create 2 `waiting` rows (needs partial unique index `(profileId) WHERE status='waiting'`, mitigated by sweep dedup); `handleLeave` unauthorized (minor); gateways emit socket.io-reserved `error` event name; legacy/auth-screen B&W color violations.
- v2 **Phases 0–4 implemented + exhaustively QA'd**: Phase 0 Expo scaffold + `@mingle/client-core`; Phase 1 v2 data model + backend domain pivot; Phase 2 onboarding + AI preference analysis; Phase 3 matchmaking queue + party formation; **Phase 4 proposal → accept → match → 1:1 messenger** — `proposal`/`match`/`messenger` modules full (propose w/ co-participation+block+cap+dup guards, accept → normalized idempotent `Match`+`DirectMessageRoom` in a Serializable tx, DM REST send/markRead/history w/ room-level read receipts, the project's **first Socket.IO gateway** `MessengerGateway`; no DB migration). Phase 4 verified live E2E (61/61) + double-accept race (40/40).
- **Phase 5a (Expo push notifications) COMPLETE + pushed**: `DeviceToken` model + `User.pushEnabled`; `PushModule`/`PushService` (lazy-loaded expo-server-sdk, best-effort NON-FATAL send + `DeviceNotRegistered` prune); `DeviceController` (caller-bound register/unregister/toggle); `NotificationService.create` fires best-effort push after the in-app row; client-core device+notification wrappers; mobile push registration + tap deep-link (`routeForNotification`) + notification-center screen + push toggle. Migration `20260707032116_phase5a_device_tokens` applied.
- **Phase 5b (DatePlan agreement) COMPLETE + pushed**: propose→select(creator)→confirm(peer)→cancel lightweight agreement flow on the existing DatePlan (no real reservation/payment). Backend `DatePlanService` gained membership+block authz (`memberContext`), `matchId` existence guard, **IDOR fix** (`GET /:id` now authz'd), `creatorProfileId`/`confirmedAt`/`cancelled`, status-guarded idempotent confirm, best-effort `reservation` notifications (`data:{datePlanId,matchId}`), all endpoints returning `DatePlanView` (no payment fields). client-core 6 wrappers; mobile `date-plan/[matchId]` state-machine screen (B&W) + chat-header entry + `routeForNotification` deep-link. Migration `20260708002715_phase5b_date_plan_agreement` applied. Whole-branch opus review READY-TO-MERGE; backend jest 141/141, client-core 49/49, mobile tsc CLEAN + vitest 7/7.
- **Phase 6c (밸런스 게임 icebreaker) COMPLETE + pushed**: the first pluggable minigame (`gameType:"balance"`). `GameService` = the whole state machine with **the DB as the only state store** (`GameSession.state` Json — restart-safe, NO migration): `start` (one active per party, 5 distinct questions from a 12-entry bank), `vote` (re-vote overwrites; **choices hidden until reveal** — snapshot carries `votedProfileIds` only; reveal+advance when ALL present roster members voted; round 5 → ended+result), `end` (force), `current` (sync). Gateway events on `PartyGateway` (authorize-first): in `game:start|vote|sync|end`, out `game:state {partyId, snapshot}`; `sync` answers the requester only; errors map Conflict/NotFound/BadRequest→`already-active`/`no-active-game`/`invalid`. client-core handle += `startGame/voteGame/syncGame/endGame` + `onGameState`. Mobile game card (start/vote/reveal/결과/다시하기, B&W) between the 2D room and chat; sync-on-join; a null sync keeps a locally-known ended summary. Final opus review READY-TO-MERGE; **live 2-socket E2E 24/24** (incl. full 5-round game flow). backend 176/176 (20 suites). ~~Backlog: start/vote RMW non-atomic~~ **FIXED (QA 2026-07-10, 892591f)**: `start/vote/end` now run in a `$transaction` guarded by a per-party `pg_advisory_xact_lock(hashtext(partyId)::bigint)` — serializes all game-state transitions per party (closes the vote lost-update/double-advance race AND the two-active-session start race), migration-free; `current()` stays lock-free (read-only).
- **Phase 6b (2D top-down party space) COMPLETE + pushed**: `@shopify/react-native-skia@2.6.2` (Expo-bundled module — the only new dep). `src/lib/party-space.ts` pure lib (normalized 0..1 coords; `clampToRoom` margin 0.06, `stepToward` 0.35/s snap-no-overshoot, `shouldEmit` dual gate 100ms+0.005Δ, `spawnFor` djb2, `initialOf`) — 12 vitest. `PartyRoomCanvas` (render-only Skia floor+avatars, RN Text initials `pointerEvents:none`, tap→normalized clamp). `party/[id].tsx`: positions in a ref + ONE rAF loop (dt-lerp all avatars; self emits `handle.move` throttled; `setFrame` only on movement — idle = zero re-renders), presence-driven spawn/prune (self exempt), move-first peers created at the reported spot with a forced frame. **NO backend/shared/client-core changes** (6a transport as-is). Final opus review READY-TO-MERGE; live gateway E2E regression 15/15; iOS+Android bundle smokes (Skia in). **GOTCHA: `pnpm install` after adding a dep wipes bcrypt's native binding (pnpm blocks build scripts) — fix `cd node_modules/bcrypt && node-pre-gyp install --fallback-to-build`.** 6c (icebreaker minigames on GameSession) still to design.
- **Phase 6a (party realtime: gateway + presence + chat) COMPLETE + pushed**: `PartyGateway` mirrors the messenger stack — JWT-handshake Socket.IO, room=partyId; in `party:join|leave|chat|move`, out `party:presence|party:message|party:moved|error`. **Every join/chat re-authorized via `PartyService.assertParticipant`** (move gated by join-time presence map); presence = in-memory `partyId→Map<socketId,profileId>` roster (distinct profileIds, empty maps pruned). Chat persists to the Phase-1 `PartyMessage` model (trim, 1–2000) — **NO migration**; history `GET /parties/:id/messages` (member-403). client-core `connectPartySocket` (injected io, auto-rejoin skipping first connect) + `getPartyMessages`; mobile party room has a live B&W chat+presence section (renders on server echo, dedup by id). `party:move` is the ephemeral transport for 6b's 2D Skia space (6b, then minigames 6c, still to design). Final opus review READY-TO-MERGE; **live 2-socket E2E 15/15** (presence/forbidden/chat/move-no-echo/history 200-403-401). backend 159/159 (19 suites); client-core 57/57; mobile tsc CLEAN + vitest 10/10. Backlog: socket `error` event name is socket.io-reserved (inherited from messenger — rename to a namespaced event across both gateways someday); in-party runtime block filtering deferred.
- **Phase 5c (mobile moderation UI) COMPLETE + pushed**: app-store 신고/차단 상시 노출. NO backend/schema change (reuses the Phase-4 safety module). client-core `reportUser` wrapper + `REPORT_REASONS`/`ReportReason`/`ReportInput`; mobile `src/lib/moderation.ts` reason labels; reusable `PeerModerationMenu` (⋯ → `Alert` menu 신고/차단, zero deps); new B&W screens `report/[profileId]` (reason picker + details ≤1000 + optional immediate block), `blocks` (list + unblock), `settings` (profile summary + block-list + logout relocated from home); ⋯ wired into chat/party(evidencePartyId)/proposals + home 설정 entry. SDD 7 tasks + whole-branch opus review = READY-TO-MERGE (0 Critical/0 Important). client-core 52/52, mobile tsc CLEAN + vitest 10/10. Backlog: report Alert destructive→iOS red (spec exempts OS Alert chrome); message-level reporting / safety-center / report-history deferred; home.tsx full B&W redesign deferred. **Native on-device E2E still pending.**
- All green: backend `nest build` clean + jest **122/122 (16 suites)** + boots (new `/devices`,`/users/me/push` routes mapped); client-core vitest **43/43**; mobile `tsc` clean + Vitest **5/5**. Phase 5a: 7 SDD tasks (each per-task reviewed) + 4 fixes (ESM boot-crash Critical, +2 client-core tests, B&W Switch, center-tap deep-link merge) + final whole-branch opus review **READY-TO-MERGE**. Native on-device push E2E still pending (needs a human `pnpm dev:mobile` on a physical device + backend up; simulators get no Expo push token).

## Conventions

- Prettier: double quotes, `trailingComma: all`, `printWidth: 100`, semicolons.
- TS: `strict`, `module: Node16`; packages are ESM (`"type": "module"`).
- Korean is used freely in commits, comments, UI copy, and planning docs.
- Auth: `JwtAuthGuard` for user routes, `AdminGuard` + `@Roles(...)` for admin; JWT payload `{ sub, email, role }`. Swagger at `/api`.
