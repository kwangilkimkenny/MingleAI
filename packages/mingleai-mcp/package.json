{
  "name": "@mingle/mingleai-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Archived MCP prototype. Decommissioned until its tools are rebuilt against the production API and authorization model.",
  "type": "module",
  "main": "dist/decommissioned.js",
  "types": "dist/decommissioned.d.ts",
  "scripts": {
    "dev": "tsx src/decommissioned.ts",
    "build": "tsc -p tsconfig.archive.json",
    "start": "node dist/decommissioned.js",
    "lint": "eslint src/decommissioned.ts --max-warnings=0",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.15.17",
    "@typescript-eslint/eslint-plugin": "^8.54.0",
    "@typescript-eslint/parser": "^8.54.0",
    "eslint": "^9.39.2",
    "tsx": "^4.19.4",
    "typescript": "^5.7.3"
  },
  "files": [
    "dist"
  ]
}
