{
  "name": "@mingle/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Archived local-SQLite prototype. Deliberately decommissioned; use @mingle/mingleai-mcp.",
  "type": "module",
  "main": "dist/decommissioned.js",
  "scripts": {
    "build": "tsc -p tsconfig.archive.json",
    "start": "node dist/decommissioned.js",
    "dev": "tsx src/decommissioned.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
