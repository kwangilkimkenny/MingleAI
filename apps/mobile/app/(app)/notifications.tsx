import { View } from "react-native";

/**
 * STUB — Task 7 must replace this with the full notification-center screen.
 * This file exists only so that expo-router typed routes resolve "/(app)/notifications"
 * and tsc does not error on references in route-for-notification.ts.
 */
export default function NotificationsScreen() {
  return <View />;
}
